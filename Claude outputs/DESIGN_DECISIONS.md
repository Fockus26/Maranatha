# DESIGN_DECISIONS

| ID | Categoría | Decisión | Razón | Estado |
|----|-----------|----------|-------|--------|
| D001 | Marca | Color principal #101B45 | Definido por el cliente | Cerrado |
| D002 | Marca | Color secundario #F9750D | Definido por el cliente | Cerrado |
| D003 | Tema | Modo oscuro y claro obligatorios | Requisito del cliente | Cerrado |
| D004 | Stack | Next.js + TypeScript + MUI + Framer Motion | Stack definido por el desarrollador | Cerrado |
| D005 | Stack | Tailwind solo para casos puntuales no resueltos por MUI | Preferencia habitual del desarrollador | Cerrado |
| D006 | Responsive | Desktop-first | Definido por el desarrollador | Cerrado |
| D007 | Estructura | Home landing (anclas) + páginas propias para Historia y Proyectos | Definido por el desarrollador | Cerrado |
| D008 | Color | Paleta completa — Opción A (fría): grises con matiz azulado, dark mode construido sobre la escala del principal (#060A1D bg / #1D2032 surface) | Coherencia con el navy de marca, evita clichés pastel/sepia religiosos, refuerza identidad en dark mode | Cerrado |
| D009 | Tipografía | Opción C ("Dinámica Variable") — Headings: Sora (600/700/800). Body: IBM Plex Sans (400/500) | Pesos variables permiten jerarquía consistente entre marketing (home) y UI (dashboard) sin sentirse dos sistemas distintos; evita serifas clásicas religiosas; integración nativa con next/font/google | Cerrado |
| D010 | Tokens | Opción A ("Angular Bold") — radius 4-16px, sombras casi planas con tinte navy (rgba 6,10,29), spacing base 4px, breakpoints desktop-first (xl 1536/lg 1200/md 900/sm 600/xs 0), z-index 0-500 | Radios ajustados y apoyo en borders sobre sombras marcadas refuerzan "audaz, vanguardista, no tradicional" sin caer en lo blando/genérico | Cerrado |
| D011 | Componentes | Sistema base — Opción A ("Bordered Editorial"): fondos sólidos, borders sobre sombras, naranja reservado para CTAs de alta prioridad (diezmo/aportes/agenda/proyectos), navy para acciones estándar | Máxima jerarquía visual para los CTAs que el cliente pidió priorizar, sin saturar de color el resto de la UI; consistente con radius/sombras de D010 | Cerrado |
| D012 | Componentes | Card de Área de Servicio y Card de Líder — foto full-bleed con overlay gradiente navy, vía componente base compartido `PhotoOverlayCard` | Mayor impacto visual/fotográfico para contenido humano (equipo, líderes) que el patrón de borde plano; compartir base evita duplicar lógica de hover/accesibilidad entre ambas cards | Cerrado |
| D013| Componentes | Social Link Card — fila horizontal, ícono en bloque `primary.50`/`primary.dark` (vía paleta de MUI, no hex fijo) que se adapta automáticamente a modo claro/oscuro, botón "Seguir/Suscribirse" en `outlined` + `color="primary"` | No es un CTA de alta prioridad (diezmo/aportes/agenda/donar), así que no lleva naranja — ese color queda reservado solo para las acciones que el cliente pidió priorizar (D011); usar paleta de MUI en vez de hex fijo evita reimplementar la lógica de dark mode por componente | Cerrado — extendido en D036 (prop `bare`); tratamiento del CTA revisado en D049 |
| D014 | Componentes | Agenda Item — date block navy fijo + fill de hover que se expande desde el borde del date block (no desde 0), texto invierte a claro con delay | Refuerza la identidad "bordered editorial" con una interacción de marca distintiva sin depender de sombras; anclar el origen del fill en el date block (en vez de en el borde izquierdo de la card) evita que el date "parpadee" o pierda su fondo durante la transición | Cerrado — sin uso actual en Home (ver D037); queda disponible para un futuro listado tipo dashboard/admin |
| D015 | Componentes | Timeline Item — año en Sora 800 como ancla visual (sin línea ni dots), con color que interpola de `primary` a `secondary` según qué tan reciente es el hito | El tratamiento tipográfico evita el cliché de timeline corporativa y refuerza "audaz/editorial" (D009); usar el gradiente cronológico hace que el naranja signifique "presente" en vez de decorar, respetando el espíritu de D011 (el secundario nunca es adorno arbitrario) y dejando ambos colores de marca como protagonistas | Cerrado — reutilizado sin cambios en Historia (Home), D040; versión con foto (`TimelineZigzagItem`) se usó en la página `/historia` (D043), ajustada en D047, página removida en D049 |
| D016 | Componentes | Project Card — dos layouts (`vertical`/`horizontal`) según el contexto (Home destacados vs listado `/proyectos`); bordered plano (no foto-overlay) porque prioriza legibilidad de cifras de recaudación sobre impacto fotográfico | D012 (foto-overlay) se reservó para contenido humano/fotográfico (áreas, líderes); acá lo que manda son datos financieros, así que se sigue el patrón bordered de YouTube Card. Alturas de título/descripción reservadas (no solo `line-clamp`) para que barra y CTA queden alineados entre cards con distinta cantidad de texto | Cerrado — layout `horizontal` puesto en uso en D044; layout `vertical` dejó de usarse en Home tras D049 (Projects.tsx ya no se monta); tratamiento del CTA "Ver proyecto" revisado en D049; adaptación mobile (apilado en columna por debajo de `sm`) en D064 |
| D017 | Componentes | Project Detail — layout de 2 columnas (contenido + sidebar sticky), con Encargados dentro del sidebar en formato fila compacta (avatar circular + nombre/rol + IG), no como PhotoOverlayCard/LeaderCard | Sidebar sticky mantiene el CTA de aporte siempre visible (prioridad D011); la fila compacta evita que el sidebar crezca demasiado con varios encargados y desplace el CTA fuera de vista, a diferencia de una versión con LeaderCard mini que sí lo hacía | Cerrado — montado en `/proyectos/[slug]`, D044; chip de estado ajustado en D047 |
| D018 | Componentes | Formulario de Pago/Diezmo — resumen fijo arriba que refleja la selección en vivo; monto por chips + campo libre mutuamente excluyentes; no captura datos de tarjeta, solo recolecta y delega el pago vía `onSubmit` | Evita el alcance de cumplimiento PCI de manejar tarjetas directamente; el resumen fijo refuerza qué se está por dar antes de completar los datos, útil sobre todo cuando se usa "Otro monto" | Cerrado |
| D019 | Componentes | Amount Selector y Donation Form Card extraídos como compartidos entre los dos formularios de contribución | Ambos formularios repetían la misma lógica de selección de monto y el mismo contenedor — extraerlos evita duplicar la lógica de mutua exclusión chip/campo libre y mantiene el estilo del card sincronizado en un solo lugar | Cerrado — `width` del card pasa a responsive en D064 (fijo solo desde `sm`) |
| D020 | Componentes | Formulario de Aporte a Proyecto — feedback de progreso en vivo (número + barra) que anima hacia el % proyectado en `success` (verde) al elegir un monto, y revierte al deseleccionar | Mostrar el impacto real del aporte en el progreso del proyecto es más persuasivo que un mensaje de texto; usar verde (en vez de naranja, reservado a CTA por D011) comunica "esto es una mejora/resultado", no una acción a tomar | Cerrado — montado en un modal desde `/proyectos/[slug]`, D044 |
| D021 | Componentes | Confirm Dialog genérico extraído para toda acción destructiva del dashboard, no solo eliminar proyecto | Evita reimplementar el mismo modal de confirmación para cada entidad del dashboard (proyectos, líderes, hitos) | Cerrado — padding interno aumentado en D065 |
| D022 | Componentes | Dashboard Table — columnas ordenables vía `TableSortLabel`, filas completadas atenuadas con `opacity`, eliminar siempre pasa por confirmación | Ordenar es necesario con más de unos pocos proyectos; atenuar (no ocultar) completados los mantiene visibles pero de menor prioridad visual; confirmar antes de eliminar previene pérdidas accidentales de datos | Cerrado — montada en `/dashboard/proyectos`, D045; adaptación mobile (tarjetas apiladas por debajo de `sm`) en D064 |
| D023 | Componentes | Dashboard Form — form único de crear/editar proyecto con presupuesto y encargados como listas editables; estado calculado automáticamente (no seleccionable); foto vía subida de archivo o URL (`ImageUploadField`); CTA "Guardar" en `primary`, no `secondary` | El estado automático evita inconsistencias entre lo que el admin marca y lo que realmente refleja el monto recaudado; separar "Guardar" del naranja mantiene ese color exclusivo para los CTAs de cara al donante (D011), no para acciones internas del dashboard | Cerrado — montado en un modal sobre la tabla, D045 |
| D024 | Navegación | Navbar con dos variantes: completo (anclas a secciones de Home + enlaces a Historia/Proyectos + CTA Diezmo) solo en Home, con scroll-spy vía `IntersectionObserver` para resaltar el ancla activa; variante simplificada (logo + enlaces a Historia/Proyectos + CTA Diezmo, sin anclas) en el resto de páginas públicas (Historia, Proyectos, detalle de proyecto) | Los enlaces de ancla no tienen sentido fuera de Home — esas secciones no existen en Historia/Proyectos como scroll targets. Dos variantes del mismo componente evitan links rotos o híbridos confusos (ej. anclas que navegarían a `/#seccion` desde otra página); en páginas no-Home, "activo" se resuelve por ruta en vez de por scroll | Cerrado — `PageNavbar` montado en Historia/Proyectos/detalle (D043/D044); `DashboardShell` montado en Dashboard (D045); enlace a Historia y CTA Diezmo por ancla removidos en D049 (página `/historia` removida, Diezmo pasa a modal); publica su altura como variable CSS en D050 (consumida por el Hero) |
| D025 | Navegación | Dashboard — sidebar fijo a la izquierda (260px), fondo navy sólido `primary[900]` permanentemente oscuro sin importar el modo activo (mismo criterio que el date block de Agenda Item, D014); ítem activo marcado con acento izquierdo 3px `secondary.main` + fondo `primary[800]` (sin fill naranja completo, respetando D011); topbar superior minimal con título de sección; ThemeToggle y "Salir" viven en el pie del sidebar | El z-index "Dashboard sidebar" (200) ya estaba reservado desde fase 03 anticipando este patrón. Mantener el sidebar navy fijo (no ligado al toggle claro/oscuro) refuerza la identidad de marca como ancla visual permanente en el dashboard, igual que ya ocurre en otros componentes; el acento en vez de fill naranja evita gastar el color reservado a CTAs de alta prioridad en un ítem de navegación | Cerrado — reemplazado por Dashboard Topbar en D059 |
| D026 | Navegación | Menú mobile — overlay fullscreen fondo navy sólido, enlaces centrados en Sora, aparición con fade + stagger 0.05s por enlace (mismo timing que Agenda Item, D014); incluye ThemeToggle y CTA Diezmo dentro del overlay; se activa por debajo del breakpoint `md` (900px); cierre por ícono X o tecla Escape | Un overlay fullscreen refuerza la dirección "audaz/editorial" en vez de un dropdown o drawer genérico; reutilizar el timing de stagger de 0.05s ya establecido mantiene consistencia de lenguaje de movimiento entre componentes del sistema | Cerrado — CTA Diezmo pasa de link por ancla a `onCtaClick` (abre modal) en D049; fondo pasa a theme-aware y links de página ganan tratamiento de píldora en D065 |
| D027 | Secciones | Home — Hero, Opción A ("Full-bleed fotográfico") como punto de partida: foto de congregación a pantalla completa + overlay navy en degradado horizontal (mismo lenguaje que `PhotoOverlayCard`, D012). Se descartaron B ("Split editorial") y C ("Tipográfico + teaser Agenda") | El full-bleed fotográfico da el mayor impacto humano/comunitario como primera impresión, coherente con D012 | Cerrado — superseded por D028 |
| D028 | Secciones | Home — Hero revisado a **slider de 3 slides** (Áreas de Servicio / Proyectos / Agenda-Prédicas) con autoplay 6s + navegación manual (flechas + barras de progreso clicables); la navegación manual reinicia el timer. Un solo CTA por slide (outline), eyebrow solo texto (11px, letter-spacing 0.18em), stats en una sola línea minimalista, headline con altura fija (3 líneas) para que CTA/stats no salten de posición entre slides, y flechas de navegación en estilo "text button" (sin fondo/borde en reposo, tenue background en hover) | Petición explícita del cliente de reducir la cantidad de información visible en el Hero; el slider permite promocionar 3 secciones de alta prioridad (D011) sin competir todas por espacio a la vez; los ajustes de espaciado/altura fija resuelven feedback directo sobre el salto visual entre slides con distinto largo de título | Cerrado — foto del Hero gana parallax en D046; su tratamiento de CTA (blanco → color al interactuar) se reutiliza como referencia para otros CTAs en D049; el `minHeight` fijo (`88vh`/`auto`) se reemplaza por `calc(100vh - navbar)` en D050; gana `maxHeight` de 1024px en D065 |
| D029 | Secciones | Home — Áreas de Servicio, Opción A ("Heading simple centrado"): eyebrow + título + copy corto, centrados, sobre el grid ya cerrado (`ServiceAreaCard`, D012). Se descartaron B ("Asimétrico 2 columnas") y C ("Numeración de sección", que habría establecido un sistema reutilizable para el resto de secciones de Home) | Tratamiento calmo y seguro que no compite con el grid de cards, que es el protagonista real de la sección; C queda como opción a retomar más adelante si se quiere un sistema de numeración para todo Home, pero no se descarta definitivamente | Cerrado — superseded por D032 (alineación del heading) |
| D030 | Estructura | Convención de archivos para `components/sections/`: **un archivo por sección**, con el componente y sus datos juntos (ej. `ServiceAreas.tsx`), en vez del patrón anterior de "grid" (datos, fase 04/05) + "wrapper" (heading, fase 06) separados. El usuario eliminó del repo los antiguos `*Grid.tsx`/`HistoryTimeline.tsx`/`ProjectSection*.tsx` y el `AreasSection.tsx` que yo había creado como wrapper — se reconstruyen como archivos consolidados a medida que se cierra cada sección | Mantiene `sections/` más limpio y legible: para ver todo lo que compone una sección (heading + datos + grid) alcanza con abrir un solo archivo, en vez de saltar entre dos | Cerrado |
| D031 | Contenido | Áreas de Servicio — se resuelve la discrepancia entre `ServiceAreaGrid` (4 áreas: Alabanza, Servicio comunitario, Niños, Evangelismo) y `HistoryTimeline`/stat del Hero (5 áreas) adoptando las 5 de Historia: Alabanza, Jóvenes, Niños, Oración, Servicio comunitario. Jóvenes reutiliza la foto de Carlos Medina (ya descrito como "Líder de jóvenes"); Oración reutiliza temporalmente la foto que antes usaba Evangelismo | El usuario eligió explícitamente "usar las 5 de Historia" entre 3 opciones planteadas (reconciliar con Historia / agregar Evangelismo como 5ta / dejarlo en 4 y ajustar el copy) — evita que Home, Historia y el Hero se contradigan en cuántas áreas de servicio existen | Cerrado — foto de Oración queda como placeholder pendiente |
| D032 | Secciones | Heading de secciones de Home (Áreas de Servicio, Liderazgo, y las que sigan) — revisado a **alineación mixta**: izquierda desde `md` (900px) en adelante, centrado de `sm` para abajo (`mx: { xs: "auto", md: 0 }`, `textAlign: { xs: "center", md: "left" }`). Se establece como patrón compartido y reutilizable para todas las secciones de Home con este tipo de heading (eyebrow + título + copy corto), no solo para Áreas de Servicio | Petición explícita del cliente sobre Áreas de Servicio ("headline a la izquierda en desktop, centrado en tablet para abajo"); se generaliza como convención del sistema en vez de resolverlo caso por caso, para que el Home no mezcle dos criterios de alineación distintos entre secciones | Cerrado — implementado en `ServiceAreas.tsx` y `Leaders.tsx`; todas las secciones de Home ganan además la animación de entrada de D046 |
| D033 | Secciones | Home — Prédicas: grid de 3 `YoutubeEmbedCard` (componente ya cerrado en fase 04, sin cambios) con el mismo heading izquierda/centrado (D032). Reproducción vía **modal (`Dialog` de MUI) con iframe embebido** de `youtube-nocookie.com` (no redirección a YouTube en pestaña nueva); se agrega un CTA "Ver canal de YouTube" (`outlined`, `color="primary"` — no es CTA de alta prioridad, D011) debajo del grid | Ambas decisiones fueron elegidas explícitamente por el cliente entre 2 opciones planteadas: mantener al usuario dentro del sitio (en vez de sacarlo a YouTube) es mejor para retención, y el CTA de canal da una salida clara para quien quiera ver más contenido sin saturar la sección con más CTAs de alta prioridad | Cerrado — `videoId`s y URL del canal quedan como contenido placeholder, pendientes de que el cliente entregue los reales; fix post-cierre: `Dialog` requería `slotProps={{ paper: {...} }}` en vez de `PaperProps` (API vieja de MUI) por la versión de MUI del proyecto — no afecta el diseño; CTA gana forma de pill en D048, pasa a acento naranja en D065 |
| D034 | Secciones/Contenido | Home — Redes Sociales: **solo Instagram** (1 `SocialLinkCard`, componente ya cerrado en fase 04, sin cambios), con el mismo heading izquierda/centrado (D032). Card contenida en `maxWidth: 420px`, alineada igual que el heading | `SocialLinkCard` es genérico y soporta cualquier plataforma, pero `PROJECT_CONTEXT.md` solo documenta Instagram — el cliente eligió explícitamente ceñirse a eso en vez de asumir presencia en Facebook/TikTok sin respaldo documental; agregar más plataformas más adelante no requiere cambios de componente, solo sumar entradas al array de datos | Cerrado — superseded por D035 (la organización tiene 3 cuentas, no 1) |
| D035 | Secciones/Contenido | Home — Redes Sociales, revisión: la organización maneja **3 cuentas de Instagram** (Iglesia Maranatha, El Evangelio Cambia, Generación JEF), no una sola. Se agrega un **selector de cuenta** sobre el `SocialLinkCard` — al elegir una cuenta, esa es la que se expande con handle + botón "Seguir". Debajo se agrega un **grid de portadas** de publicaciones de la cuenta activa; cada portada es un link que abre el post real en Instagram en pestaña nueva | El usuario corrigió el alcance original (D034) tras ver el preview: no es una cuenta, son 3, cada una con su propio contenido. Sobre 2 decisiones que se le plantearon explícitamente: (a) click en una portada abre el post real en Instagram — un link normal, no requiere API de Meta; (b) 4 posts por cuenta en vez de 6, por mantener el grid compacto. Se descartó deliberadamente cualquier reproducción de reels o integración oficial con la API de Meta — el propio usuario pidió no hacerlo si implicaba esa complejidad | Cerrado — superseded por D036 (selector rediseñado + portadas más altas) |
| D036 | Secciones/Componentes | Home — Redes Sociales, revisión de estilo del selector: los chips sueltos + card aparte (D035) pasan a ser **tabs fusionadas con el panel** — la pestaña de la cuenta activa comparte fondo (`background.paper`) y esquinas superiores redondeadas con el panel de abajo (sin borde entre ambos), dando el efecto de que el panel "sale" de la pestaña activa (referencia: pestañas de Chrome). Transición animada vía `flex-grow` (la pestaña activa crece ligeramente, 1 → 1.3) + fade/slide corto con Framer Motion al cambiar de cuenta. `SocialLinkCard` gana la prop opcional `bare` (default `false`, no rompe usos existentes) para renderizarse sin su borde/fondo propio y encajar dentro del panel sin doble-card. Las portadas de publicaciones pasan de proporción 1:1 a **4:5** (más altura, más fiel al grid real de Instagram), manteniendo 4 columnas en `md`+ y 2 en `xs`/`sm` | Petición explícita del cliente: cards de post más altas "como las de IG", y que el selector de cuentas y el `SocialLinkCard` se sintieran "como si fueran un solo componente" en vez de dos piezas sueltas, con una referencia concreta al efecto de pestañas de Chrome | Cerrado — implementado en `SocialLinks.tsx` y en `SocialLinkCard.tsx` (prop `bare`) |
| D037 | Secciones/Componentes | Home — Agenda: en vez de una lista vertical uniforme (`AgendaItem`, D014), la sección usa un **grid tipo mosaico ("bento")** con `grid-auto-flow: dense`, donde cada evento ocupa una celda de tamaño distinto según su naturaleza: el evento puntual más próximo es la celda **"feature"** (2×2, más grande, tag "Evento especial" en texto naranja); los demás puntuales son celdas **"wide"** (2×1, bordeadas); el Servicio Dominical (ancla semanal) es una celda **"tall"** (1×2); Oración y Jóvenes son celdas **"sm"** (1×1) compactas y repetidas. Se implementa como subcomponente local `AgendaTile` dentro de `Agenda.tsx` (no se modifica `AgendaItem`, que queda cerrado y disponible para un futuro listado tipo dashboard/admin). El toggle "Ver toda la agenda (N)"/"Ver menos" pasa de recortar en 4 a recortar en 6 (mejor encaje con el mosaico) | Petición explícita del cliente: quería la sección "más como un calendario, pero no estático" — con eventos de distinto ancho/alto para sentirse más dinámica — y pidió creatividad libre sobre el criterio de tamaños. Se priorizó visualmente lo más próximo y lo menos frecuente (eventos puntuales) sobre lo repetitivo (recurrentes), y dentro de las recurrentes se le dio más peso al Servicio Dominical por ser el servicio principal de la semana | Cerrado — implementado en `Agenda.tsx`; criterio de tamaños es una decisión de diseño propia, abierta a ajuste si el cliente prefiere otro criterio; el tratamiento de color de la celda "feature" fue revisado en D039, animación de expandir/contraer y color de la celda destacada revisados en D047, animación reemplazada de nuevo en D049 |
| D038 | Componentes | Home — Agenda: la celda cuya fecha coincide con **hoy** recibe el mismo tratamiento navy sólido que la celda "feature" (fondo `primary.main`, texto claro, tag propio "Hoy"), aunque no sea el evento puntual más próximo. El **hover** de cualquier celda que no esté ya destacada (ni "feature" ni "hoy") replica exactamente ese mismo estado navy en vez del lift + borde naranja que tenía antes | Petición explícita del cliente: quería que el hover tuviera "el mismo estilo que cuando el día actual es el de la agenda". Como ese estado "hoy" no existía todavía, se le preguntó qué aspecto debía tener — eligió el fondo navy sólido entre esa opción y una alternativa de borde naranja marcado | Cerrado — superseded por D039 (el cliente pidió revertir el hover a su diseño anterior y acotar el navy a un solo evento por mes) |
| D039 | Componentes | Home — Agenda, revisión sobre D038: el **hover** vuelve al diseño de D037 (lift + sombra + borde naranja), sumándole un fondo sutil (`action.hover`) — ya no imita ningún estado navy. El **"destacado"** (fondo navy sólido, texto claro) deja de depender de "hoy" y pasa a ser **un único evento por mes: el recurrente de fecha más próxima** (nunca un puntual) — si ese servicio/reunión es hoy, su tag dice "Hoy"; si el próximo cae más adelante (p. ej. hoy no hay servicio pero sí mañana), dice "Próximo evento". Los eventos puntuales (celda "feature"/"wide") ya **no** llevan destacado navy — vuelven al fondo bordeado normal, conservando solo su tag naranja "Evento especial" | Petición explícita del cliente: no le gustaba que el hover imitara el estado "hoy"/"evento próximo" (D038) — pidió mantener el diseño de hover anterior sumándole solo un fondo sutil, y aclaró que el "destacado" navy debe seguir la lógica de "el evento recurrente más próximo, sea hoy o el siguiente día con servicio" — nunca un evento especial, que solo debe llevar su label naranja | Cerrado — implementado en `Agenda.tsx` (`AgendaTile` gana `isAccented` en vez de derivar todo de `isFeature`/`isToday`; `findAccentKey` ubica el único evento recurrente más próximo); color de fondo y texto del destacado revisados en D047 |
| D040 | Secciones | Home — Historia (resumen): sección nueva, archivo consolidado `History.tsx` (D030) que reutiliza `TimelineItem` (D015) **sin modificarlo**, mostrando solo **4 hitos** (subconjunto, no la línea de tiempo completa) seguidos de un CTA "Ver historia completa" (`outlined`, `color="primary"` — no lleva naranja, D011) hacia `/historia`. Heading con el mismo tratamiento izquierda/centrado (D032) que el resto de Home | La página completa `/historia` es fase 07 (`PAGE_INVENTORY.md`) — el Home solo necesita un teaser, así que se reutiliza el componente ya cerrado en vez de construir una versión nueva; limitar a 4 hitos mantiene la sección breve, coherente con que es un "resumen" y no la fuente de verdad completa | Cerrado — implementado en `History.tsx`; contenido de los hitos (años/títulos/descripciones) es placeholder, construido a partir del stat "11 años sirviendo" del Hero — pendiente de que el cliente confirme la línea de tiempo real; el antiguo `HistoryTimeline.tsx` (que definía las 5 áreas, D031) fue eliminado por el usuario y no se recuperó su contenido original; CTA "Ver historia completa" removido en D049 (página `/historia` removida — este resumen es ahora la única superficie de Historia) |
| D041 | Secciones | Home — Proyectos (destacados): sección nueva, archivo consolidado `Projects.tsx` (D030) que reutiliza `ProjectCard` en layout `vertical` (D016) **sin modificarlo**, mostrando **3 proyectos destacados** (2 activos + 1 completado, para exhibir ambos estados del componente) en grid de 3 columnas. Cada card conserva su propio CTA "Aportar"/"Ver proyecto" (naranja/outline, resuelto por el componente); debajo del grid, un CTA de sección "Ver todos los proyectos" (`outlined`, `color="primary"` — no lleva naranja, D011) hacia `/proyectos`. Heading con el mismo tratamiento izquierda/centrado (D032) que el resto de Home | La página completa `/proyectos` (listado + detalle) es fase 07 (`PAGE_INVENTORY.md`) — el Home solo necesita destacados, así que se reutiliza `ProjectCard` ya cerrado en vez de construir una card nueva; mostrar un proyecto completado junto a los activos deja ver que el sistema de estados (D016) funciona correctamente sin inventar contenido adicional | Cerrado — implementado en `Projects.tsx`; los 3 proyectos (títulos, descripciones, fotos, montos) son placeholder, pendientes de que el cliente entregue los proyectos reales o de que existan en el dashboard (D023, fuera de alcance de esta fase); los links de CTA a `/proyectos` y `/proyectos/[slug]` apuntan a páginas que todavía no existían — resuelto en fase 07 (D044). **Superseded por D049**: esta sección deja de montarse en Home (`Projects.tsx` queda sin usar) — Proyectos vive únicamente en `/proyectos` |
| D042 | Secciones | Home — CTA Diezmo/Aportes: última sección de Fase 06, archivo `Tithe.tsx` (D030). A diferencia de todas las demás secciones de Home, usa **fondo navy sólido fijo** (`#101B45`), sin importar el modo claro/oscuro — mismo criterio que el sidebar del dashboard (D025) y el overlay del menú mobile (D026) — para que se sienta como el cierre del recorrido y el CTA de más alta prioridad (D011). Layout de 2 columnas: copy a la izquierda (texto claro sobre navy), `TitheForm` (D018/D019) **sin modificar** a la derecha, cuyo `DonationFormCard` blanco flota sobre el fondo navy. `id="diezmo"` coincide con `TITHE_ANCHOR_ID` (`navItems.ts`) — a diferencia de Historia/Proyectos (páginas propias), esta sí es un ancla real de Home | El navy fijo (en vez de heredar `background.default`/`paper` como el resto de secciones) refuerza visualmente que esta es la sección de mayor prioridad del sitio — mismo lenguaje ya usado para otros elementos de marca permanentes (D025/D026); reutilizar `TitheForm` tal cual evita duplicar la lógica de validación/mutua exclusión de montos ya resuelta en D018/D019 | Cerrado — implementado en `Tithe.tsx`; `onSubmit` es un placeholder (`console.log`, sin proveedor de pago integrado) — la integración de pago real queda fuera de alcance de esta fase. Con esta sección se completa la **Fase 06 — Secciones de Home** (`CURRENT_PHASE.md`). Post-cierre: el cliente encontró la card del formulario muy angosta (`DonationFormCard` tenía un ancho fijo de 320px, D019) — se agregó una prop opcional `width` a `TitheForm` (default `undefined`, no rompe otros usos) que se reenvía a `DonationFormCard`; `Tithe.tsx` la usa con `440px`. Fondo "wow" (patrón animado + contador) decidido en D048. **Superseded por D049**: esta sección deja de montarse en Home — su contenido se traslada a `components/ui/TitheModal.tsx`, abierto como modal global |
| D043 | Páginas | Historia (`/historia`, fase 07) — layout **zigzag**: cada hito alterna foto grande + texto (izquierda/derecha), con año en `secondary.main` (Sora 800) sobre el título/descripción. Nuevo componente `components/ui/TimelineZigzagItem.tsx` — no se modificó `TimelineItem` (D015), que sigue en uso sin cambios en el resumen de Home. Se amplió a **6 hitos** (2015, 2017, 2019, 2022, 2024, 2026) — más que los 4 del resumen, ya que esta sí es la fuente completa. Se presentaron 2 opciones comparadas visualmente (mini-artifact con toggle): lista vertical extendida sin fotos vs. zigzag con fotos — el cliente eligió zigzag | El cliente pidió ver ambas opciones antes de decidir; el zigzag da más peso fotográfico/humano a la historia completa, coherente con la dirección "audaz/editorial" (`DESIGN_RULES.md`) — a costa de requerir una foto por hito, hoy placeholder (fotografía de stock de Unsplash sin relación con la iglesia real) | Cerrado — implementado en `app/historia/page.tsx`; crop de la foto ajustado en D047. **Superseded por D049**: la página se remueve (redirect a Home) — la línea de tiempo completa vuelve a vivir únicamente como el resumen de Home |
| D044 | Páginas | Proyectos — listado (`/proyectos`, fase 07) con filtro por **tabs** ("Todos"/"Activos"/"Completados"), elegido por el cliente entre 3 opciones (tabs / chips+orden / sidebar de filtros) porque hoy solo existe un dato categórico real (estado). Usa `ProjectCard` en layout `horizontal` (D016) sin modificar. Proyecto — detalle (`/proyectos/[slug]`) monta `ProjectDetailContent` + `ProjectSidebar` (D017) sin modificar ninguno; el botón "Aportar" del sidebar abre un modal con `ProjectContributionForm` (D020) precargado con foto/título/montos del proyecto — `onSubmit` es placeholder (mismo criterio que D042, sin proveedor de pago). Slug no encontrado → `notFound()` estándar de Next (no es un "estado de error" propio de la fase). Se creó `lib/projectsData.ts` como fuente única de proyectos (6 registros, incluye los 3 ya destacados en Home con los mismos montos/slugs, D041) | El listado necesitaba el layout `horizontal` de `ProjectCard`, ya diseñado para este uso desde D016 pero sin página que lo montara; centralizar los datos en `lib/projectsData.ts` evita que Home, el listado y el detalle tengan cifras/fotos inconsistentes para el mismo proyecto | Cerrado — implementado en `app/proyectos/page.tsx`, `app/proyectos/[slug]/page.tsx`, `app/proyectos/[slug]/ProjectDetailClient.tsx`; color del tab activo y chip del sidebar ajustados en D047. Es ahora la **única** superficie de Proyectos del sitio (D049 — Home ya no duplica destacados). Tab activo y chip "Activo" (en ambos, tarjeta y sidebar) revisados de nuevo en D050. Toggle lista/cuadrícula agregado en D052, removido en D065 (una sola vista) |
| D045 | Páginas | Dashboard (fase 07) — Resumen (`/dashboard`): tarjetas de KPI (recaudado total, proyectos activos, proyectos completados) + mini-lista de próximos cierres, elegido por el cliente entre 3 opciones (stats+próximos cierres / solo redirect / solo stats). Proyectos (`/dashboard/proyectos`): `DashboardTable` (D022) + `DashboardProjectForm` (D023) para crear/editar abierto en un **modal** sobre la tabla, elegido por el cliente entre 3 opciones (modal / rutas dedicadas / vista inline) — eliminar ya pasaba por `ConfirmDialog` (D021), no se dupicó. `DashboardShell` (D024) se monta en cada página (no en un layout compartido) porque su prop `title` varía entre "Resumen" y "Proyectos". Nuevo `lib/dashboardProjectsStore.tsx`: estado de proyectos en memoria (React Context), sembrado desde `lib/projectsData.ts` (D044) y compartido entre ambas páginas del dashboard vía `DashboardProjectsProvider` en `app/dashboard/layout.tsx` | No hay backend todavía — sin esto, cada página del dashboard tendría su propia copia de los datos y una edición en "Proyectos" no se reflejaría en el "Resumen" durante la misma sesión. El Context es la solución más simple que evita ese desajuste sin construir persistencia real, que queda fuera de alcance de esta fase | Cerrado — implementado en `app/dashboard/layout.tsx`, `app/dashboard/page.tsx`, `app/dashboard/proyectos/page.tsx`, `lib/dashboardProjectsStore.tsx`. **Limitación conocida, a resolver cuando exista backend**: crear/editar/eliminar en el dashboard no modifica el listado público (`/proyectos`) ni Home — ambos siguen leyendo el array estático de `lib/projectsData.ts`; se pierde todo al recargar. Autenticación del Dashboard (login) tampoco se implementó — fuera de alcance de esta fase |
| D046 | Secciones | Home — ritmo visual (feedback del cliente: "todo se ve muy igual" al bajar por Home, faltaba un efecto wow). Se presentaron 3 direcciones comparadas visualmente (canvas de `/design`): A) ritmo tonal + reveal al scroll, B) anclas fotográficas con parallax, C) sistema editorial numerado. El cliente eligió **B, integrando las animaciones de A**. Implementado como: (1) nuevo `components/ui/PhotoAnchorBand.tsx` — franja fotográfica full-bleed con parallax real (`framer-motion` `useScroll`/`useTransform`, la foto se mueve más lento que el scroll) + un stat grande superpuesto sobre overlay navy; se insertan 2 instancias en `app/page.tsx`, entre Áreas de Servicio → Liderazgo ("+400 · personas sirviendo cada semana") y entre Agenda → Historia ("11 años · construyendo comunidad"); (2) parallax también sumado a la foto del propio `Hero.tsx` (D028), que ahora se sobredimensiona ±10% para que el desplazamiento no revele bordes; (3) nuevo `components/ui/Reveal.tsx` — wrapper de fade + slide-up (0.7s, curva suave, dispara una sola vez al entrar en viewport) envolviendo el heading y el contenido principal (con un `delay` leve entre ambos) de **todas** las secciones de Home con heading eyebrow/título/copy (`ServiceAreas`, `Leaders`, `Sermons`, `SocialLinks`, `Agenda`, `History`, `Projects`, `Tithe`) | El cliente pidió ver opciones antes de decidir sobre un cambio que afecta a Home completo, no a una sección puntual; eligió la dirección con mayor impacto fotográfico (parallax) pidiendo además sumar el movimiento de la opción más sutil (reveal al scroll), en vez de una sola de las tres tal cual se mostraron — se combinaron ambas técnicas sin tocar el resto del sistema de color/tipografía (D008/D009) ni los componentes ya cerrados de cada sección | Cerrado — implementado en `Hero.tsx`, `app/page.tsx`, `components/ui/PhotoAnchorBand.tsx`, `components/ui/Reveal.tsx` y las 8 secciones listadas. Contenido placeholder: las 2 fotos de `PhotoAnchorBand` son stock de Unsplash sin relación con la iglesia real, mismo criterio que el resto del sitio. `Reveal.tsx` gana `amount` responsive (más bajo en mobile) en D065 |
| D047 | Secciones/Componentes/Páginas | Ronda de feedback post-fase 07 sobre detalles puntuales: (1) **Agenda** — la celda "destacada" pasa de `primary.main` (variable por modo, se veía "lavado"/azul claro en modo oscuro) a navy fijo `#101B45` (mismo criterio D014/D025); día, mes, título y hora dentro de esa celda pasan todos al mismo blanco sólido `#F5F6FA` (antes mes/hora usaban una opacidad reducida distinta a día/título). Expandir/contraer la agenda ahora anima con `framer-motion` (`AnimatePresence` + `layout` en cada celda: fade+scale al entrar/salir, reacomodo suave del resto del mosaico) en vez de saltar; el ícono del botón "Ver toda la agenda"/"Ver menos" rota 180° animado en vez de cambiar de golpe. (2) **Historia** (`TimelineZigzagItem`) — `backgroundPosition` de la foto pasa de `center` a `center top`, para no cortar el sujeto cuando está en el tercio superior de la imagen. (3) **Proyectos** (`/proyectos`) — el tab activo pasa de `text.primary` (se veía navy/azul) a `secondary.main` (naranja), coherente con el indicador de tabs que ya era naranja. (4) **Project Sidebar** — el chip "Activo" pasa de `primary.main` (mismo problema de "lavado" en modo oscuro que la Agenda) a navy fijo `#101B45` con texto blanco, más peso de fuente (700) y letter-spacing, para resaltar más sobre la card blanca/paper | Feedback puntual del cliente tras revisar la fase 07 en conjunto: fondo/texto de la Agenda no se sentían sólidos (mismo patrón de "navy fijo" que otros elementos de marca permanentes del sistema, D014/D025/D026, que esta celda no estaba siguiendo); expandir/contraer sin transición se sentía brusco; el crop centrado de las fotos de Historia cortaba el contenido relevante; el tab activo de Proyectos y el chip del sidebar no comunicaban selección/estado con suficiente contraste | Cerrado — implementado en `Agenda.tsx`, `TimelineZigzagItem.tsx`, `app/proyectos/page.tsx`, `ProjectSidebar.tsx`. Efecto "wow" de `Tithe.tsx` y variación de los CTAs repetidos, decididos y cerrados en D048. Animación de Agenda reemplazada de nuevo en D049. Tab activo y chip "Activo" revisados de nuevo en D050 (el fix de acá no fue suficiente en modo oscuro) |
| D048 | Secciones | Efecto "wow" de Diezmo/Aportes y variación de los CTAs repetidos — decisión final tras el canvas comparativo de `/design` (3 opciones cada uno, ver D046/D047). **Diezmo (`Tithe.tsx`)**: el cliente combinó las opciones B + C — (1) patrón geométrico diagonal muy sutil sobre el navy sólido, en pan continuo vía CSS `@keyframes` (`patternPan`, 24s linear infinite, sin foto); (2) contador animado ("+132 familias aportando este año") que cuenta hacia arriba una sola vez al entrar en viewport (`useInView` de `framer-motion`, `once: true` — mismo criterio que `Reveal`), debajo de la descripción. **CTAs repetidos**: el cliente eligió la opción B (tratamiento distinto por sección, no un único patrón para las 3) — Proyectos conserva el botón outline con flecha (sin cambios); Historia (`History.tsx`) pasa a link de texto subrayado sin caja (`variant="text"`, borde inferior que aparece en hover); Prédicas (`Sermons.tsx`) pasa a pill redondeada (`borderRadius: "999px"`) para marcar visualmente que ese link sale del sitio (YouTube), a diferencia de los otros dos que son navegación interna | El cliente vio las 3 opciones de cada comparativo en el canvas de `/design` antes de decidir — para Diezmo prefirió combinar dos técnicas en vez de una sola (textura de fondo + dato en movimiento, sin foto); para los CTAs prefirió que cada sección tuviera su propia firma visual en vez de una única solución aplicada a las 3, mientras Proyectos (el único con grid inmediatamente arriba, más "cargado") se queda con el tratamiento de botón más visible que ya tenía | Cerrado — implementado en `Tithe.tsx`, `History.tsx`, `Sermons.tsx`. `FAMILIES_SUPPORTING = 132` es contenido placeholder, mismo criterio que el resto del sitio. El fondo/contador de `Tithe.tsx` se traslada a `TitheModal.tsx` en D049; el CTA de Historia queda sin efecto al removerse la página `/historia` (D049) |
| D049 | Secciones/Páginas/Componentes | Ronda de feedback post-fase 07 (2): (1) **Se remueve la página `/historia`** — la línea de tiempo completa vuelve a vivir únicamente como el resumen ya existente en Home (`History.tsx`); la ruta queda como `redirect("/")` en vez de eliminarse del árbol de archivos (esta sesión no tiene capacidad de borrar archivos), y su CTA "Ver historia completa" se quita de `History.tsx` (ya no hay página completa a la que enlazar). El link "Historia" se quita de `PAGE_NAV_ITEMS` (`navItems.ts`). (2) **Se remueve la sección Proyectos de Home** (`Projects.tsx` deja de montarse en `app/page.tsx`) — Proyectos pasa a vivir únicamente en `/proyectos`, sin duplicarse en Home. (3) **Diezmo pasa de sección a modal global**: `Tithe.tsx` (D042/D048, con su fondo navy + patrón animado + contador) se quita de Home y su contenido se traslada a un nuevo `components/ui/TitheModal.tsx` (`Dialog` de MUI), controlado por un Context nuevo — `TitheModalProvider`/`useTitheModal()` en `lib/titheModalStore.tsx` — montado una sola vez en `app/layout.tsx` junto a `{children}`. El botón "Diezmo" del `Navbar`, `PageNavbar` y `MobileMenuOverlay` deja de navegar a un ancla (`#diezmo`/`/#diezmo`) y pasa a llamar `openTithe()`; se quita `TITHE_ANCHOR_ID` de `navItems.ts`. `MobileMenuOverlay` cambia su prop `ctaHref: string` por `onCtaClick: () => void`. (4) **CTAs "Ver todos los proyectos"/"Ver proyecto"/"Seguir cuenta"** — el cliente no quedó conforme con el tratamiento outline plano (`color="primary"`) de estos botones; pasan a imitar el CTA del Hero (D028): neutros en reposo (borde `divider`, texto `text.secondary`) y al interactuar toman color — azul/navy (`primary.main` + `alpha(primary.main, 0.08)` de fondo) en vez del naranja que usa el Hero sobre foto. Aplicado en `ProjectCard.tsx` (rama "Ver proyecto"/completado — "Aportar" no cambia, sigue siendo el CTA `secondary` de alta prioridad, D011) y `SocialLinkCard.tsx` ("Seguir/Suscribirse"). (5) **`ServiceAreaCard`** pierde el badge de ícono (`topLeftSlot`) y el CTA "Conocer más" (`bottomSlot`'s `motion.span`) — la card ahora solo muestra foto + nombre + descripción; `icon`/`ctaLabel` se mantienen en la interfaz sin uso, para no forzar cambios en `ServiceAreas.tsx`. (6) **Animación de Agenda, reemplaza a D047**: el enfoque de montar/desmontar tiles con `AnimatePresence`+`layout` (D047) se sentía abrupto (abre de golpe, al cerrar los tiles se desvanecen y luego colapsa) — se reemplaza por una técnica de "altura revelada": la grilla visible ahora renderiza SIEMPRE todos los eventos (sin `.slice()`), envuelta en un contenedor `overflow: hidden` cuya altura anima entre un valor "colapsado" y uno "completo", medidos con `ResizeObserver` sobre dos grillas ocultas idénticas (mismo `gridAutoFlow: dense`). `AgendaTile` vuelve a ser un `Box` plano (se quita `motion(Box)`/`layout`/`initial`/`animate`/`exit`/`whileHover`, hover vuelve a CSS puro); el chevron del botón toggle conserva su rotación animada | Segunda ronda de feedback puntual del cliente sobre la fase 07: pidió simplificar la arquitectura del sitio (Historia solo en Home, Proyectos solo en su página) en vez de duplicar contenido entre Home y páginas propias; pidió que Diezmo, al ser el CTA de mayor prioridad del sitio (D011), interrumpa con un modal accesible desde cualquier página en vez de requerir navegar a Home y hacer scroll; señaló que el color de los CTAs "Ver proyecto"/"Seguir cuenta" no le convencía y pidió que se pareciera al patrón interactivo del Hero (blanco → color al interactuar) pero en azul; pidió quitar ícono y CTA de las cards de Áreas de Servicio, sin dar razón adicional; y describió explícitamente cómo debía sentirse la animación de la Agenda ("los elementos siempre estuvieron ahí y se van descubriendo/tapando"), lo que llevó a descartar el enfoque de mount/unmount de D047 por uno de altura animada sobre contenido siempre presente | Cerrado — implementado en `app/historia/page.tsx` (redirect), `navItems.ts`, `History.tsx`, `app/page.tsx`, `lib/titheModalStore.tsx` (nuevo), `components/ui/TitheModal.tsx` (nuevo), `app/layout.tsx`, `Navbar.tsx`, `PageNavbar.tsx`, `MobileMenuOverlay.tsx`, `ProjectCard.tsx`, `SocialLinkCard.tsx`, `ServiceAreaCard.tsx`, `Agenda.tsx`. `Tithe.tsx` y `Projects.tsx` quedan como archivos sin usar en `components/sections/` (esta sesión no puede eliminar archivos) — su contenido vive ahora en `TitheModal.tsx` y en la página `/proyectos` respectivamente. El tratamiento de CTA "Hero-style" quedó sin aplicar en `ProjectSidebar.tsx` (misma etiqueta "Ver proyecto", instancia distinta) — completado en D050 |
| D050 | Componentes/Secciones/Páginas | Tercera ronda de feedback puntual, items "sencillos" que habían quedado sueltos tras D049: (1) **Modal de Diezmo a pantalla completa**: `TitheModal.tsx` pasa de `fullScreen` solo por debajo de `md` a `fullScreen` fijo en cualquier tamaño — en desktop se veía como un dialog centrado de ancho medio, ahora ocupa toda la pantalla siempre, con el contenido centrado verticalmente dentro de ese espacio. (2) **Hero ocupa exactamente el resto del viewport bajo el navbar**: en vez de un `88vh` fijo (D028) o `auto` en mobile, `Navbar.tsx` ahora mide su propia altura real vía `ResizeObserver` y la publica como variable CSS `--navbar-height` en `:root`; `Hero.tsx` usa `minHeight: calc(100vh - var(--navbar-height, 72px))` en vez de un porcentaje fijo o adivinado — se ajusta solo si el navbar cambia de alto. (3) **CTA "Ver proyecto" del `ProjectSidebar`** (página de detalle de proyecto) — quedó sin el tratamiento Hero-style (neutro en reposo, azul al interactuar) que sí se había aplicado a `ProjectCard.tsx` y `SocialLinkCard.tsx` en D049; se completa acá con el mismo patrón. (4) **Tab activo de `/proyectos` en naranja**: el override manual vía sx (`& .Mui-selected { color: "secondary.main !important" }`, D047) se reemplaza por los props nativos de MUI `textColor="secondary"` / `indicatorColor="secondary"` en el propio `<Tabs>` — forma soportada directamente por el componente, más confiable que pelear con especificidad CSS a mano. (5) **Chip "Activo" — contraste en modo oscuro**: tanto en `ProjectSidebar.tsx` (D047) como en `ProjectCard.tsx` (rama sin foto del layout `horizontal`, que usaba `theme.palette.primary.main` sin fijar), el navy más profundo (`primary[700]`, `#101B45`) quedaba casi del mismo valor que la superficie oscura del card (`gray[800]`, `#1D2032`) y no resaltaba. En modo oscuro ambos chips ahora usan `primary[600]` (un peldaño más claro/saturado en la misma escala navy) en vez de `primary[700]`, más un borde sutil claro (`alpha("#F5F6FA", 0.14)`) que marca el borde del chip incluso cuando el contraste tonal es bajo | El cliente marcó explícitamente que estos eran ajustes "sencillos" en comparación con rondas anteriores más estructurales (D049) y pidió no perderlos de vista: modal de diezmo no ocupaba toda la pantalla en desktop; el Hero no llenaba el alto completo del viewport (dejaba espacio muerto o se pasaba, según breakpoint); uno de los tres botones "tipo Hero" pedidos en la ronda anterior (D049) quedó sin aplicar por ser una instancia distinta del mismo texto "Ver proyecto" (`ProjectSidebar` vs. `ProjectCard`); el tab activo de Proyectos, pese al fix de D047, no se percibía naranja de forma confiable; y el chip "Activo" seguía sin resaltar en modo oscuro pese al navy fijo ya aplicado en D047, porque ese navy fijo resultó ser casi del mismo valor que la superficie oscura del card | Cerrado — implementado en `components/ui/TitheModal.tsx`, `components/layout/Navbar.tsx`, `components/sections/Hero.tsx`, `components/ui/ProjectSidebar.tsx`, `components/ui/ProjectCard.tsx`, `app/proyectos/page.tsx` |
| D051 | Componentes/Global | Cuarta ronda de feedback puntual sobre D050: (1) **Tab activo de `/proyectos` — texto menos intenso**: el color de `.Mui-selected` baja de `secondary.main` (`#F9750D`, vía `textColor="secondary"` nativo de MUI, D050) a `secondary[600]` (`#DC5F02`) — un naranja más apagado/oscuro que mantiene la identidad de marca sin saturar el texto; el indicador (línea de 2px) se queda en `secondary.main` a toda intensidad vía `indicatorColor="secondary"`, ya que ahí sí funciona bien por ser una línea delgada, no texto. (2) **Scroll suave global**: se agrega `scroll-behavior: smooth` a `html` en `app/globals.css` (con `@media (prefers-reduced-motion: reduce)` que lo desactiva para quien lo pidió a nivel sistema) — antes SOLO el menú mobile hacía scroll suave (`scrollIntoView({behavior:"smooth"})` explícito en `MobileMenuOverlay`); los enlaces de ancla de escritorio (`Navbar`, `Footer`, CTAs del Hero hacia `#areas`/`#agenda`) no tenían ningún `scroll-behavior` definido y saltaban de golpe. (3) **CTA "Ver canal de YouTube" (`Sermons.tsx`)**: era el botón "sencillo" que quedó sin tocar en las dos rondas anteriores (D049/D050) — pese a las menciones repetidas, seguía siendo el `outlined`/`color="primary"` plano de siempre (con el único cambio visual de D048: forma de pill). Se reemplaza por el mismo patrón que ya tienen `ProjectCard.tsx`, `ProjectSidebar.tsx` y `SocialLinkCard.tsx`: neutro en reposo (borde `divider`, texto `text.secondary`) y acento navy al interactuar (borde/texto `primary.main` + fondo `alpha(primary.main, 0.08)`) — imitando el patrón del CTA del Hero (D028) con azul en vez de naranja. Esto reemplaza el radio de pill distintivo de D048: el cliente pidió aplicar "los mismos estilos" a los tres botones, priorizando la consistencia entre ellos sobre la firma visual propia que D048 le había dado a este CTA en particular | El cliente marcó tres cosas puntuales: el texto del tab activo de Proyectos seguía sintiéndose "muy naranja" incluso después de D050 (el `textColor="secondary"` nativo de MUI usa `secondary.main`, el tono más vivo de la escala); el scroll al hacer click en enlaces de la propia página saltaba en vez de deslizarse, algo que D047/D049/D050 nunca habían cubierto porque el único lugar con scroll suave real era el menú mobile; y remarcó, ya por tercera vez, que un botón seguía sin el tratamiento acordado — terminó siendo el CTA de Prédicas (`Sermons.tsx`), la única instancia "tipo botón repetido" (D048) que nunca había recibido el ajuste Hero-style aplicado a las otras tres | Cerrado — implementado en `app/proyectos/page.tsx`, `app/globals.css`, `components/sections/Sermons.tsx`. Acento del CTA de Prédicas cambia de navy a naranja en D065 (para no chocar con el hover naranja de las cards) |
| D052 | Componentes/Global | Quinta ronda de feedback: (1) **Flash del divider del navbar en la carga inicial**: `ThemeRegistry.tsx` siempre arrancaba `mode` en `"light"` y recién corregía a la preferencia real (guardada en `localStorage` o del sistema) en un `useEffect`, así que si esa preferencia era `"dark"` el navbar se pintaba primero con el divider claro (`gray[200]`, casi invisible) y luego, de golpe, con el oscuro — dos renders en vez de uno. Se agrega un script bloqueante en `app/layout.tsx` (dentro de `<head>`, se ejecuta antes de que React hidrate) que resuelve el modo y lo deja en el atributo `data-color-mode` de `<html>`; `ThemeRegistry.tsx` ahora inicializa su `useState` leyendo ese atributo (lazy initializer) en vez de partir siempre de `"light"`, así el primer render del cliente ya llega con el modo correcto. (2) **Vista lista/cuadrícula en `/proyectos`**: se agrega un toggle (dos `IconButton` con el mismo tratamiento "neutro en reposo, acento navy activo/hover" que el resto de controles del sitio) que alterna entre el layout `horizontal` de `ProjectCard` (el listado de una columna que ya existía) y su layout `vertical` en grilla de hasta 3 columnas (el mismo que usa el resumen de Home en `components/sections/Projects.tsx`) — no se crea ningún componente nuevo, solo se alterna entre los dos layouts que `ProjectCard` ya soportaba (D016). (3) **Animación de layout**: tanto el cambio de tab (Todos/Activos/Completados, que agrega o quita proyectos filtrados) como el cambio lista/cuadrícula ahora animan con `framer-motion` — el contenedor y cada `ProjectCard` llevan `layout` (FLIP automático de posición/tamaño) y los items entran/salen con fade + scale vía `AnimatePresence` (`mode=popLayout`); mismo criterio de easing/duración (`[0.2, 0.8, 0.2, 1]`, ~0.35–0.45s) que `Reveal.tsx` y `Agenda.tsx` |El cliente reportó que el divider del navbar "aparece de golpe" con el color equivocado al cargar la página — el modo claro/oscuro se resolvía en dos pasos (uno en el servidor/primer render, siempre claro, y otro corregido después por JS), visible como parpadeo. También pidió poder ver el listado de `/proyectos` en dos formatos (el actual y uno tipo tarjeta/grilla) y que hubiera animación tanto al cambiar de tab como al cambiar de formato de visualización | Cerrado — implementado en `app/layout.tsx`, `theme/ThemeRegistry.tsx`, `app/proyectos/page.tsx`. **Superseded en D065**: el toggle lista/cuadrícula se quita a pedido del cliente — vuelve a quedar una sola visualización (la `horizontal`/lista, que era la de por defecto) |
| D053 | Global | Sexta ronda: el flash del divider del navbar (D052) seguía visible — el fix anterior (script bloqueante en `<head>`, cliente) corría antes de que React hidratara, pero el HTML que manda el SERVIDOR seguía saliendo siempre en `mode="light"` (SSR no tiene forma de conocer la preferencia real sin más información), así que si la preferencia real era "dark" el navegador pintaba primero el divider claro y luego, de golpe, el oscuro — exactamente lo que el cliente seguía viendo. Se reemplaza por resolución en el SERVIDOR vía cookie: `theme/ThemeRegistry.tsx` ahora escribe la preferencia tanto en `localStorage` (como antes) como en una cookie `color-mode` (1 año, `SameSite=Lax`) cada vez que se guarda; `app/layout.tsx` pasa a ser un Server Component async que lee esa cookie con `cookies()` (`next/headers`) ANTES de renderizar y le pasa el modo resuelto a `ThemeRegistry` como prop `initialMode`, que lo usa directamente como estado inicial — el HTML que sale del servidor ya viene en el modo correcto, sin nada que corregir después. Se agrega también una migración de una sola vez (`useEffect` en `ThemeRegistry`) para quien ya tenía preferencia en `localStorage` pero aún no cookie: la sincroniza para que la SIGUIENTE carga ya la resuelva el servidor (en esa carga particular sí puede verse un salto único, costo de migración, no algo que se repita). Única excepción que queda, inherente a este enfoque: la primerísima visita de alguien, antes de que exista la cookie, usa "light" por defecto | El cliente confirmó que, tras D052, el divider del navbar seguía apareciendo blanco/claro y luego atenuándose al color correcto — el fix anterior solo movía CUÁNDO se resolvía el modo dentro del cliente, pero no evitaba que el servidor siguiera mandando siempre HTML en modo claro; la única forma de eliminar el flash por completo es que el propio servidor ya sepa la preferencia real al generar el HTML, lo cual requiere una cookie en vez de (o adicional a) `localStorage`, que el servidor no puede leer | Cerrado — implementado en `app/layout.tsx`, `theme/ThemeRegistry.tsx` |
| D054 | Componentes | Séptima ronda: el cliente notó que en Home los 5 links-ancla (Áreas, Liderazgo, Prédicas, Redes, Agenda) y el único link-página ("Proyectos") se veían idénticos en el navbar — mismo texto plano, mismo peso, misma fila — así que alguien entrando por primera vez podía pensar que "Proyectos" era también una sección de esa misma página; y que al entrar a una página interna (`/proyectos`, `/historia`) no había ningún link explícito de regreso a Inicio, solo el logo. Se armó un comparador de 3 direcciones distintas en `/design` (canvas con mockups fieles al sistema de diseño real) y el cliente eligió la Opción 1: **separador + píldora**. Implementado en `Navbar.tsx` (Home) y `PageNavbar.tsx` (resto de páginas): los links-página pasan de texto plano subrayado a una píldora con borde 1px (`divider`), radio `999px` e ícono `OpenInNewRounded` (comunica "esto te lleva a otro lugar" sin depender solo del color); en Home se agrega un divisor vertical entre el grupo de anclas y el grupo de páginas. `PageNavbar` ahora agrega siempre un primer link "Inicio" (ícono `HomeRounded`) con el mismo tratamiento de píldora, tanto en escritorio como en el menú mobile (`MobileMenuOverlay`, vía `mobileLinks`) — antes el mobile de páginas internas tampoco tenía forma explícita de volver. El estado activo (la página en la que ya estás, solo aplica en `PageNavbar`) usa `secondary[600]` (el mismo naranja apagado del tab seleccionado de `/proyectos`, D051) en vez del naranja vivo, por el mismo motivo: como estado persistente, el naranja vivo se sentía demasiado intenso | El cliente reportó dos problemas de UX relacionados: (1) nada distinguía visualmente un link que hace scroll dentro de la página actual (ancla) de uno que navega a otra página — mismo estilo de texto para ambos — lo cual podía confundir a un visitante nuevo sobre qué es "Proyectos"; (2) al entrar a una página interna no había ningún link explícito de "Inicio", solo el logo (que muchos usuarios no identifican como clickeable). Se le mostraron 3 propuestas visuales antes de implementar, para que eligiera la dirección con la que se sentía más cómodo en vez de asumir una | Cerrado — implementado en `components/layout/Navbar.tsx`, `components/layout/PageNavbar.tsx`. El mismo criterio de píldora se lleva al menú MOBILE (que hasta entonces no distinguía anclas de páginas) en D065 |
| D055 | Global | Octava ronda: el cliente pidió una transición al navegar entre páginas (Home ↔ Historia ↔ Proyectos ↔ detalle de proyecto), que hasta ahora cambiaban de golpe (comportamiento default de Next.js App Router). Se agrega `components/layout/PageTransition.tsx` — un client component que envuelve `{children}` en `app/layout.tsx` con `AnimatePresence` (`framer-motion`) usando `usePathname()` como `key`: al cambiar de ruta, la página saliente hace fade + slide-up corto de salida y la entrante el mismo movimiento de entrada (0.3s, easing `[0.2, 0.8, 0.2, 1]` — la misma curva que `Reveal.tsx`/`Agenda.tsx`, pero más rápida por ser navegación y no entrada de contenido en scroll). `initial={false}` en `AnimatePresence` evita animar la carga inicial del sitio (cada página ya tiene su propia entrada vía `Reveal`); solo anima navegaciones posteriores. Respeta `prefers-reduced-motion` igual que el scroll suave global (`app/globals.css`, D053) — con esa preferencia activada el cambio de página es instantáneo. El `TitheModal` queda deliberadamente FUERA del wrapper (sigue siendo hijo directo de `TitheModalProvider`, no de `PageTransition`) para que si ya estaba abierto al navegar no se vea afectado por la animación de la página de atrás | El cliente pidió explícitamente una transición entre páginas — el sitio no tenía ninguna, los cambios de ruta eran instantáneos/abruptos, inconsistente con el resto de animaciones ya establecidas (reveals al hacer scroll, layout animado en Proyectos) | Cerrado — implementado en `components/layout/PageTransition.tsx`, `app/layout.tsx` |
| D056 | Global | Fix a D055: el cliente reportó que la transición dejaba la página de Proyectos sin mostrar sus elementos (tarjetas) al navegar hacia ella — tenía que recargar para verlas. Causa: D055 envolvía `{children}` en `app/layout.tsx` con `AnimatePresence` (`mode="wait"`) usando `usePathname()` como key, para animar salida Y entrada; pero `/proyectos` ya trae su PROPIO `AnimatePresence` (`mode="popLayout"`, D052 — anima tabs y lista↔cuadrícula) y anidar un `AnimatePresence` en modo `wait` alrededor de una página que internamente también usa `AnimatePresence` dejaba esa página a medio montar. Se cambia de enfoque: en vez de animar salida+entrada a nivel global, se anima solo la ENTRADA de cada página, usando el mecanismo nativo de Next.js `app/template.tsx` (a diferencia de `layout.tsx`, se vuelve a montar en cada navegación) — ya no hace falta `AnimatePresence` ni `usePathname` a nivel de transición, así que no hay forma de chocar con el `AnimatePresence` interno de Proyectos ni el de ninguna otra página. `PageTransition.tsx` se simplifica a un solo fade + slide-up de 0.3s en el montaje; `app/layout.tsx` vuelve a su estado de D053 (sin envolver `{children}`) | El cliente reportó el bug tal cual: la transición hacia Proyectos rompía el renderizado de sus tarjetas, con la página quedando en blanco hasta recargar — un problema de robustez, no de gusto visual, así que se resolvió cambiando de mecanismo en vez de ajustar timings | Cerrado — implementado en `components/layout/PageTransition.tsx`, `app/template.tsx`, `app/layout.tsx` |
| D057 | Componentes | Novena ronda: primer lote de feedback sobre el modal de Diezmo/Aportes (`TitheModal.tsx`, D049), el cliente avisó que habrá más rondas después de esta. (1) **Se quita el reflejo naranja del formulario**: el overlay `radial-gradient(circle at 82% 15%, rgba(249,117,13,0.10)...)` sobre el fondo navy (pensado como textura sutil, D048) se veía como un reflejo no intencional sobre la tarjeta del formulario — se elimina por completo, queda solo el patrón geométrico en pan (`patternPan`). (2) **Navy de fondo más acentuado**: pasa de `primary[700]` (`#101B45`) a `primary[900]` (`#060A1D`) — el cliente lo sentía "muy claro"; combinado con las líneas blancas semitransparentes del patrón, el navy se percibía menos saturado de lo esperado. (3) **Acentos naranjas más acentuados**: eyebrow ("Da con propósito"), ícono y contador de familias pasan de `secondary.light` (`#FCA355`, pastel) a `secondary.main` (`#F9750D`, el naranja de marca) — mismo motivo, se veían apagados. (4) **Animación tipo contador al cambiar el monto**: nuevo hook `useAnimatedAmount` en `TitheForm.tsx` (usa `animate()` de `framer-motion`, mismo easing `[0.2,0.8,0.2,1]`, 0.45s) — el monto grande de arriba ya no salta de golpe entre valores, cuenta de forma animada; si el origen o el destino es 0 salta directo (no tiene sentido "contar" desde/hacia cero). (5) **Texto naranja del preset seleccionado, más claro ("se veía sucio")**: en `AmountSelector.tsx`, el preset activo usaba `theme.palette.secondary.dark` (`#B34C02`) sobre fondo naranja translúcido, lo que se leía como un naranja quemado/marrón poco legible — se reemplaza por el token `secondary[600]` (`#DC5F02`), el mismo naranja apagado ya usado como "seleccionado" en el resto del sitio (tab de Proyectos D051, píldora activa del navbar D054). (6) **Animación de layout en Tipo/Frecuencia, igual que los tabs de Proyectos**: nuevo componente `components/ui/SegmentedToggle.tsx` reemplaza el `ToggleButtonGroup` nativo de MUI (que no trae indicador deslizante) — control propio con un fondo compartido vía `layoutId` de `framer-motion` que hace FLIP entre la opción anterior y la nueva (mismo mecanismo, en espíritu, que el indicador nativo de `Tabs`); usado para Tipo (Diezmo/Ofrenda) y Frecuencia (Única vez/Mensual) en `TitheForm.tsx`. El fondo activo usa `primary.main` (navy). Los presets de `AmountSelector.tsx` reciben el mismo tratamiento (`layoutId` propio, aislado vía `useId()`). (7) **Placeholders/inputs más chicos**: los `TextField` del formulario (nombre, correo, monto libre) bajan de 16px (default de MUI en reposo) a `typography.size.small` (14px) — mismo tamaño que el resto de texto secundario del formulario. (8) **Se quitan las flechas de incremento/decremento del input de monto libre**: `-webkit-appearance`/`-moz-appearance: none` sobre los spinners nativos del `input[type=number]` | El cliente revisó el modal de Diezmo ya implementado (D049) y mandó una lista puntual de 8 ajustes antes de avanzar a la siguiente fase, aclarando que habrá más rondas de feedback sobre este mismo modal más adelante — se resolvieron los 8 siguiendo convenciones ya establecidas del sistema (`secondary[600]` para estados "seleccionado" activos de D051/D054, easing `[0.2,0.8,0.2,1]` compartido, patrón `layoutId` ya usado en Proyectos D052) en vez de introducir criterios nuevos | Cerrado — implementado en `components/ui/TitheModal.tsx`, `components/ui/TitheForm.tsx`, `components/ui/AmountSelector.tsx`, `components/ui/SegmentedToggle.tsx` (nuevo) |
| D058 | Componentes | Décima ronda: segundo lote de feedback sobre el modal de Diezmo, sobre lo implementado en D057. (1) **Animación del monto — de deslizamiento a fundido**: el highlight naranja de los presets (`AmountSelector.tsx`) usaba un `layoutId` compartido entre los tres botones, lo que hacía que framer-motion animara el fondo como si fuera un único elemento deslizándose (FLIP) de la posición del preset anterior a la del nuevo — el cliente pidió explícitamente que NO se deslizara, sino que el preset anterior se desvaneciera en su lugar mientras el nuevo aparece en el suyo. Se quita el `layoutId` compartido; cada preset ahora tiene su propio `AnimatePresence` local con `initial`/`animate`/`exit` de solo opacidad (0.22s) — mismo resultado visual de "seleccionado" pero como fundido cruzado, sin movimiento entre casillas. (2) **Animación para la primera selección de monto**: `useAnimatedAmount` (`TitheForm.tsx`) tenía un caso especial que saltaba directo a `target` (sin contar) cuando el monto anterior era 0 — pensado para evitar "contar desde cero" en cualquier transición hacia/desde 0, pero esto también desactivaba el conteo en el caso más común: la primera vez que el usuario elige un monto. Se acota el caso especial a solo cuando el DESTINO es 0 (deseleccionar/limpiar) — cualquier otra transición, incluida 0 → primer monto, ahora cuenta animado igual que el resto. Además, el bloque del monto grande (`TitheForm.tsx`) gana un `AnimatePresence` propio con `key` en si hay monto válido o no (no en el valor del monto) — cubre el salto de texto "—" → "$25" con un fundido + scale-in de 0.22s, ya que ese cambio de placeholder a número no es interpolable por el contador; los cambios de monto entre sí (25→50) siguen sin retriggerear esta animación, solo el conteo. (3) **Navy "lavado" en modo oscuro**: el fondo sólido `#060A1D` del modal (D057) se veía diluido/lavado SOLO en modo oscuro, bien en modo claro — causa: `Dialog` de MUI monta su `Paper` interno con `elevation=24` por defecto, y el `Paper` de MUI en modo oscuro aplica automáticamente un overlay blanco translúcido proporcional a la elevación (para simular profundidad/cercanía a la luz); en modo claro ese overlay no existe, por lo que el color sólido se veía correcto solo ahí. Se agrega `elevation: 0` al `slotProps.paper` del `Dialog` en `TitheModal.tsx`, que desactiva el overlay por completo — el navy sólido queda intacto en ambos modos | Segunda ronda de feedback puntual sobre el modal de Diezmo (el cliente ya había avisado que vendría más tras D057): remarcó que la animación de layout del monto no debía deslizarse entre presets, sino hacer fundido; notó que faltaba animación específicamente para el caso "sin monto → primer monto" (el conteo existente saltaba ese caso); y reportó que el fondo navy seguía viéndose lavado en modo oscuro pese al ajuste de D057, aclarando que en modo claro sí se veía bien — ese detalle (el problema es exclusivo de un modo) apuntó directo al overlay de elevación de `Paper`, comportamiento automático de MUI en dark mode, no al valor de color en sí | Cerrado — implementado en `components/ui/AmountSelector.tsx`, `components/ui/TitheForm.tsx`, `components/ui/TitheModal.tsx` |
| D059 | Componentes/Páginas | Rediseño completo del Dashboard (Resumen + Proyectos) — el cliente no estaba conforme con el diseño anterior (D024/D025/D045) y pidió ver 3 direcciones distintas antes de elegir. Se armó un comparador de 3 direcciones en `/design` (mockups fieles al sistema de diseño real, datos de ejemplo): A) "Sidebar Editorial" (evolución del sidebar+topbar actual, con tendencias y feed de actividad), B) "Panel Superior" (sin sidebar fijo, barra navy horizontal + banda de KPIs a pantalla completa) y C) "Workspace de Tarjetas" (rail de íconos + mosaico bento + cards de proyecto, reutilizando patrones ya aprobados del sitio público). El cliente eligió la **Opción B**. Implementación: se reemplaza `DashboardSidebar` (fijo, 260px, D024/D025) por un nuevo `components/layout/DashboardTopbar.tsx` — barra navy sólida horizontal (mismo `primary[900]` permanente sin importar el modo, mismo criterio D024/D025) con navegación Resumen/Proyectos como píldoras (activa en `secondary[600]`, el mismo naranja apagado de "seleccionado" ya usado en D051/D054, en vez de un acento lateral de 3px) — libera los 260px que antes ocupaba el sidebar para el contenido. Nuevo `components/ui/DashboardStatBand.tsx`: banda navy a pantalla completa inmediatamente debajo del topbar con los KPIs principales, reutilizando el mismo patrón geométrico en pan continuo de `TitheModal.tsx` (D048/D057) en vez de inventar una textura nueva. `DashboardShell.tsx` se simplifica a solo topbar + `<main>` sin padding propio (cada página resuelve su estructura, ya que Resumen necesita la banda de borde a borde). Resumen (`app/dashboard/page.tsx`) mantiene únicamente los 3 KPI que el store ya calculaba (total recaudado, activos, completados) — se descartó agregar el 4º KPI de ejemplo del mock ("Aportes este mes") por no tener un dato real detrás — y suma una tarjeta de "Acciones rápidas" (Nuevo proyecto vía `?new=1`, Ver todos los proyectos, meta acumulada calculada). Proyectos (`app/dashboard/proyectos/page.tsx`) gana su propio encabezado (título + conteo real de activos/completados) y un buscador por título que filtra la tabla existente en el cliente (el mock de `/design` mostraba un buscador; acá se conecta a algo funcional en vez de dejarlo decorativo) — `DashboardTable`/`DashboardProjectForm` (D022/D023) no cambiaron. `DashboardSidebar.tsx` queda sin uso (esta sesión no puede eliminar archivos) | El cliente pidió explícitamente 3 direcciones distintas para el dashboard completo antes de decidir, en vez de una sola propuesta — vio las 3 en `/design` y eligió la que menos se parecía al panel-admin genérico que tenía antes, reutilizando en cambio el lenguaje navy+patrón ya validado en otras partes del sitio (Diezmo). El buscador y el 4º KPI del mock se resolvieron con criterio propio al pasar de mockup a implementación real: uno se conectó a datos reales por ser trivial y útil, el otro se omitió por no tener datos reales que mostrar | Cerrado — implementado en `components/layout/DashboardTopbar.tsx` (nuevo), `components/ui/DashboardStatBand.tsx` (nuevo), `components/layout/DashboardShell.tsx`, `app/dashboard/page.tsx`, `app/dashboard/proyectos/page.tsx`. Espaciado del topbar ajustado para mobile en D064 |
| D060 | Componentes/Páginas | Ronda de feedback sobre la Opción B del Dashboard (D059) recién implementada. (1) **Sin transición en hover**: los botones "Nuevo proyecto" y "Ver todos los proyectos" de Resumen no tenían `transition` — el cambio de color/fondo en hover saltaba de golpe; se agrega `theme.transitions.create(...)` a ambos (mismo criterio que el resto de botones del sistema). (2) **"Nuevo proyecto" ya no navega**: antes llevaba a `/dashboard/proyectos?new=1` para abrir el formulario ahí (redundante con el botón "Ver todos los proyectos", que sí debe navegar) — el modal de crear/editar proyecto pasa de estado local de `app/dashboard/proyectos/page.tsx` a un Context global nuevo (`lib/dashboardProjectModalStore.tsx`, `useDashboardProjectModal`) con el modal (`components/ui/DashboardProjectModal.tsx`) montado una sola vez en `app/dashboard/layout.tsx` — mismo patrón que `TitheModal`/`titheModalStore` (D049). Así "Nuevo proyecto" de Resumen abre el modal directamente sin navegar, y Proyectos (tanto su propio botón "Nuevo proyecto" como "editar" de cada fila) usa el mismo modal compartido. Ya no hace falta el query param `?new=1` ni el `Suspense`/`useSearchParams` que requería. (3) **Espaciado insuficiente entre el topbar y el contenido de Proyectos**: el padding superior (20px) se sentía pegado a la barra navy — se sube a 32px/48px (mobile/desktop). Resumen no se tocó: ahí la banda de KPI está pensada para ir pegada al topbar (D059). (4) **Chips "Activo"/"Completado" apagados**: el texto de "Activo" usaba `theme.palette.secondary.dark` (`#B34C02`), el mismo naranja "sucio" ya corregido en `AmountSelector` (D058) — pasa a `secondary[600]`; el fondo de ambos chips sube de 10% a 15% de opacidad para más contraste. (5) **Animación de layout en la tabla al crear/eliminar**: `DashboardTable` dejó de ser un `<Table>` semántico de MUI y pasó a una "tabla" armada con CSS Grid (`role="table"/"row"/"cell"` para mantener la semántica accesible) — animar `<tr>` con `framer-motion` no es confiable entre navegadores (los transforms que usa `layout` para el FLIP no se aplican de forma consistente sobre elementos de tabla), mientras que sobre `Box`/grid sí funciona igual que en el resto del sitio (Proyectos, D052). Las filas ahora llevan `layout` + `AnimatePresence` (fade + scale, 0.28s, easing `[0.2,0.8,0.2,1]`) al crear o eliminar un proyecto. (6) **Modal de crear/editar con scroll interno**: antes el `Dialog` usaba `scroll="body"` y crecía con el contenido (scrolleaba la página completa); pasa a altura fija `95vh` con scroll interno del `DialogContent` (`scroll="paper"`, el default de MUI). Además `DashboardProjectForm` gana una prop `bare` (mismo patrón que `SocialLinkCard`, D036) que quita su borde/fondo/padding/ancho-máximo propios cuando vive dentro de `DashboardProjectModal` — antes se veía como una tarjeta blanca flotando sobre el fondo de otra tarjeta (el `Paper` del `Dialog`), con espacio de más alrededor | Primer lote de feedback del cliente sobre la Opción B recién implementada — puntual, sobre detalles de interacción y micro-UX más que de dirección visual: transiciones faltantes, un botón que no debería navegar, espaciado insuficiente, colores de chip apagados (mismo patrón de "naranja sucio" ya visto en D058), falta de animación de layout donde el resto del sitio ya la tiene, y un modal que no se comportaba como tal (crecía con la página en vez de tener su propio scroll interno, y se veía como una tarjeta dentro de otra) | Cerrado — implementado en `components/ui/DashboardProjectForm.tsx`, `components/ui/DashboardProjectModal.tsx` (nuevo), `components/ui/DashboardTable.tsx`, `lib/dashboardProjectModalStore.tsx` (nuevo), `app/dashboard/layout.tsx`, `app/dashboard/page.tsx`, `app/dashboard/proyectos/page.tsx`. **Superseded parcialmente en D065**: el modal pasa de `95vh` centrado a `fullScreen` |

| D061 | Componentes/Global | Ronda de feedback sobre la Opción B del Dashboard (D059/D060), octavo lote: (1) **Animación de layout en la píldora activa del topbar** (`DashboardTopbar.tsx`) y **subrayado deslizante en el navbar de Home** (`Navbar.tsx`): ambos pasan de un fondo/borde estático que solo cambiaba de color a un indicador compartido vía `layoutId` (mismo criterio que `SegmentedToggle.tsx`, D057) que se desliza entre el ítem anterior y el activo. Limitación conocida y documentada en el propio código: el topbar del dashboard se remonta en cada navegación entre `/dashboard` y `/dashboard/proyectos` porque vive debajo de `app/template.tsx` (D055/D056, que envuelve TODO el árbol, incluyendo `app/dashboard/layout.tsx`, para poder animar la entrada de cada página) — así que entre páginas la píldora reaparece con fade en vez de deslizarse; el deslizamiento real sí ocurre dentro de una misma carga. Resolverlo del todo requeriría sacar `/dashboard` del alcance de `app/template.tsx` (p. ej. vía route groups), lo que implica mover archivos de ruta existentes — no viable en esta sesión (sin `device_bash`, sin capacidad de borrar/mover archivos). El navbar de Home no tiene esta limitación (no depende de navegación, es scroll-spy dentro de una página siempre montada), así que ahí el deslizamiento es real. (2) **Transición de página**: se revisó `app/template.tsx`/`PageTransition.tsx` (D055/D056) y ya cubre TODAS las rutas del sitio, incluyendo `/dashboard` y `/dashboard/proyectos` (fade + slide-up de 0.3s al montar cada página) — no se encontró ningún punto donde este mecanismo no aplicara, así que no se agregó nada nuevo para no duplicar la animación ya existente; si el cliente sigue sin percibirla en el dashboard puntualmente, es candidato a revisar con capturas o en vivo en la próxima ronda. (3) **Botón "Nuevo proyecto" en blanco** (`app/dashboard/proyectos/page.tsx`): usaba `color="primary"` de MUI, que en modo oscuro toma `primary.contrastText` (`gray[900]`, casi negro) — se agrega `sx={{ color: "#FFFFFF" }}` para texto blanco fijo en cualquier modo, mismo criterio que el botón equivalente de Resumen (que ya lo tenía hardcodeado). (4) **Encabezados de columna de la tabla, tamaño reducido**: al pasar de `<TableCell>` (que MUI dimensiona a ~14px por defecto) a `Box role="columnheader"` sobre CSS Grid (D060), estos encabezados quedaron sin `fontSize` propio y heredaban el cuerpo ambiente (16px) — se agrega `fontSize: 13` explícito, igual que la columna "Acciones" que ya lo tenía. (5) **Chips "Activo"/"Completado", fondo sólido**: el ajuste de opacidad de D060 (10%→15%) seguía sin resultar suficiente para el cliente — se reemplaza el criterio de fondo translúcido + texto de acento por fondo SÓLIDO (`secondary[600]`/`success.main`) + texto blanco, mismo tratamiento de contraste que la píldora activa del topbar. (6) **Fondo más oscuro en modo oscuro para `ConfirmDialog` y `DashboardProjectModal`**: mismo diagnóstico que `TitheModal` (D057/D058) — el `Paper` del `Dialog` de MUI usa `elevation={24}` por defecto y en modo oscuro aplica automáticamente un overlay blanco translúcido proporcional a la elevación, aclarando el fondo más de lo esperado; se agrega `elevation: 0` en `slotProps.paper` de ambos. (7) **Botón "Editar" deshabilitado en proyectos completados**: en `DashboardTable.tsx`, el `IconButton` de editar ahora solo se renderiza cuando `!isCompleted` — "Eliminar" sigue disponible en cualquier estado. (8) **Scroll personalizado**: se agrega a `app/globals.css` (`scrollbar-width`/`scrollbar-color` + `::-webkit-scrollbar*`), aplicado globalmente (sin acotar a un selector, por lo que también cubre el scroll interno del `DialogContent` del modal de proyecto, que ya trae `overflow-y: auto` por defecto de MUI, D060). El color del thumb no sigue `prefers-color-scheme` del sistema sino el modo REAL de la app: se agrega un `useEffect` en `theme/ThemeRegistry.tsx` que sincroniza el atributo `data-color-mode` en `<html>` con el `mode` del toggle (D053), y `globals.css` lee ese atributo | Octavo lote de feedback puntual del cliente sobre el Dashboard: pidió animación de layout en ambos navbars al cambiar de ítem activo (dashboard y Home), una transición de página, texto blanco fijo en un botón concreto, encabezados de tabla más chicos, chips todavía apagados pese al ajuste anterior (D060), fondo más oscuro en dos modales específicos en modo oscuro, deshabilitar "Editar" en proyectos completados, y un scrollbar personalizado en todo el sitio y dentro del modal. Se investigó a fondo la arquitectura de `template.tsx` de Next.js antes de implementar el punto (1): confirmar que fuerza el remonte de todo lo que envuelve (incluidos layouts anidados como `app/dashboard/layout.tsx`) en cada navegación evitó prometer un deslizamiento perfecto entre páginas que esta sesión no puede entregar sin poder mover archivos de ruta — se documentó la limitación en vez de ocultarla | Cerrado — implementado en `components/layout/Navbar.tsx`, `components/layout/DashboardTopbar.tsx`, `app/dashboard/proyectos/page.tsx`, `components/ui/DashboardTable.tsx`, `components/ui/ConfirmDialog.tsx`, `components/ui/DashboardProjectModal.tsx`, `app/globals.css`, `theme/ThemeRegistry.tsx`. Pendiente para una futura ronda: si el cliente confirma que sigue sin percibir la transición de página en `/dashboard`, revisar en vivo — el mecanismo (`app/template.tsx`) ya cubre esa ruta según el código |

| D062 | Componentes | Ronda de feedback sobre el modal de crear/editar proyecto (`DashboardProjectForm.tsx`) y el diálogo de confirmación de borrado, noveno lote: (1) **Nombre del proyecto en negrita al eliminar**: `ConfirmDialog.tsx` cambia su prop `description` de `string` a `ReactNode` (antes solo podía renderizar texto plano vía `DialogContentText`) para poder resaltar el nombre del proyecto; `DashboardTable.tsx` arma el mensaje como JSX envolviendo el título en un `Box component="span"` con `fontWeight: 700`. (2) **Placeholders/labels del modal, tamaño reducido**: todos los `TextField` del formulario (título, descripción, montos, líneas de presupuesto, datos de encargados) heredaban el default de MUI (16px) — se agrega un `fieldSx()` compartido (`fontSize: 14px` en input/label/helper-text) a cada uno, mismo criterio ya aplicado en `TitheForm.tsx` (D057). (3) **Flechas de incremento/decremento del monto**: se quitan de todos los inputs numéricos del formulario (meta, recaudado, monto de cada línea de presupuesto) vía `-webkit-appearance`/`-moz-appearance: none` sobre los spinners nativos — mismo fix ya hecho en el monto libre de `TitheForm.tsx` (D057), esta vez extendido a los 3 campos numéricos de este formulario. (4) **Input de fecha personalizado**: nuevo componente `components/ui/DateField.tsx` reemplaza al `TextField type="date"` nativo de MUI. MUI no trae un date picker propio sin agregar `@mui/x-date-pickers` como dependencia nueva, y esta sesión no puede correr `npm install` en la máquina del cliente (sin `device_bash`) — así que se mantiene el `<input type="date">` nativo (conserva el selector del sistema operativo, accesible, sin librerías nuevas) pero envuelto en un contenedor con el mismo lenguaje visual del resto de inputs (borde, radius, alto 40px, foco en `primary.main`), ocultando el ícono nativo del navegador (`::-webkit-calendar-picker-indicator` a opacidad 0) y mostrando en su lugar `CalendarMonthRoundedIcon`; todo el contenedor es clickeable y abre el picker nativo vía `showPicker()`. Usado en "Fecha de cierre". (5) **Se quita el chip "Activo"/"Completado" del formulario**: se mostraba junto al campo "Monto recaudado", calculado en vivo a partir de meta/recaudado — el cliente señaló que no tiene sentido mostrar dentro del formulario un estado que el propio formulario genera; el campo vuelve a ocupar el ancho completo (deja de compartir fila con el chip). (6) **Animación al agregar/eliminar líneas de presupuesto y encargados**: ambas listas pasan a envolver sus items en `AnimatePresence` + `layout` (fade + scale + slide corto, 0.22s, mismo easing `[0.2,0.8,0.2,1]` del resto del sitio) — mismo patrón ya usado en `DashboardTable.tsx` para crear/eliminar filas (D060). (7) **Botón "Guardar" con texto blanco**: mismo ajuste que el botón "Nuevo proyecto" de Proyectos (D061) — `color="primary"` + modo oscuro usaba `primary.contrastText` (casi negro); se agrega `sx={{ color: "#FFFFFF" }}`. (8) **Rediseño de la foto del proyecto**: `ImageUploadField.tsx` pasa de una vista previa cuadrada chica (96×96px) al costado de los controles, a un banner a todo el ancho con `aspectRatio: 16/9` arriba y los controles (tabs + botón/URL) debajo — mismo contenedor navy de antes como placeholder cuando no hay foto, ahora con un ícono `ImageOutlinedIcon` centrado en vez de quedar vacío | Noveno lote de feedback puntual del cliente sobre el modal de crear/editar proyecto: pidió resaltar el nombre del proyecto en el mensaje de confirmación de borrado, placeholders más chicos, quitar las flechas de los inputs numéricos, un selector de fecha propio (preguntó explícitamente si MUI tenía alguna solución — se investigó y se optó por no agregar una dependencia nueva que esta sesión no puede instalar), quitar el chip de estado por no tener sentido en un formulario, animación al agregar/quitar líneas de presupuesto y encargados, texto blanco en "Guardar", y una vista previa de foto más grande | Cerrado — implementado en `components/ui/ConfirmDialog.tsx`, `components/ui/DashboardTable.tsx`, `components/ui/DashboardProjectForm.tsx`, `components/ui/DateField.tsx` (nuevo), `components/ui/ImageUploadField.tsx` |

| D063 | Componentes | Ronda de feedback sobre el modal de crear/editar proyecto, sobre lo implementado en D062 — décimo lote. (1) **Animación de quitar líneas de presupuesto/encargados**: al cliente le gustó la animación de agregar pero no la de eliminar — se quita el prop `exit` de ambos `motion.div` (`DashboardProjectForm.tsx`); sin `exit`, `AnimatePresence` desmonta el item de inmediato sin animar su salida, mientras el resto de la lista se sigue reacomodando suavemente vía `layout` (que no depende de `exit`). (2) **Hueco a la derecha del label al flotar**: causa — el `fieldSx()` de D062 apuntaba solo a `.MuiInputLabel-root`, pero el ancho del "notch" del borde (`NotchedOutline`, variante `outlined` de MUI) se calcula con `font-size: 0.75em` relativo al tamaño de fuente del CONTENEDOR, no del label; como el contenedor seguía en el tamaño por defecto (16px) mientras el label ya medía 14px, el notch quedaba más ancho que el label real, dejando el hueco visible. Se cambia `fieldSx()` para fijar `fontSize` a nivel RAÍZ del `TextField` (hereda por CSS a input, label y notch a la vez) en vez de apuntar a un selector anidado — resuelve el hueco en los ~12 campos del formulario que lo usan. (3) **Botón/input de foto con la misma altura**: en `ImageUploadField.tsx`, el botón "Elegir imagen" (`size="small"` de MUI, ~30.75px) y el `TextField` de URL (`size="small"`, ~40px) tenían alturas distintas — cambiar entre las tabs "Subir archivo"/"URL" corría el resto del formulario hacia arriba o abajo. Se fija `height: 40px` en ambos de forma explícita (vía `sx` en el botón, vía `.MuiInputBase-root` en el `TextField`) para que el contenedor no salte de alto al alternar. (4) **Calendario propio de verdad**: la primera versión de `DateField.tsx` (D062) mantenía el `<input type="date">` nativo dentro de un contenedor con estilo propio, pero al hacer click seguía abriendo el selector del sistema operativo/navegador sin estilizar — el cliente aclaró que quería un calendario con diseño propio al abrirse, no solo un trigger estilizado. Se reescribe `DateField.tsx` de cero como un dropdown propio construido con `Popper`/`ClickAwayListener` (ya incluidos en MUI, sin sumar ninguna librería de calendario/fechas nueva — mismo criterio de D062 de no poder correr `npm install` en la máquina del cliente) + matemática de fechas simple con `Date` nativo (semana en lunes, grilla de 6×7 con días del mes anterior/siguiente atenuados, día actual marcado con un borde sutil, día seleccionado en `secondary.main` sólido, header con mes/año en español y flechas para navegar entre meses). El trigger pasa de mostrar el `<input>` nativo a un texto formateado (`"15 mar 2026"`, vía `Intl.DateTimeFormat`) o el placeholder "Seleccionar fecha". El valor se sigue guardando como string `YYYY-MM-DD`, sin cambios para quien consume el formulario | Décimo lote de feedback puntual del cliente sobre el modal de crear/editar proyecto, todo sobre lo recién implementado en D062: distinguió explícitamente que la animación de agregar sí le gustaba pero la de quitar no (en vez de pedir quitar ambas); señaló un defecto visual concreto (hueco a la derecha del label al flotar) que se rastreó hasta cómo MUI calcula el ancho del notch del borde outlined, no un problema del valor de fuente en sí; pidió que el botón e input de la sección de foto no cambiaran la altura del layout al alternar; y aclaró que el pedido anterior de "calendario personalizado" no se había cumplido — el input v1 seguía abriendo el picker nativo del navegador, la intención siempre fue un calendario con diseño propio | Cerrado — implementado en `components/ui/DashboardProjectForm.tsx`, `components/ui/ImageUploadField.tsx`, `components/ui/DateField.tsx` (reescrito) |

| D064 | Global/Componentes | **Fase 08 — Adaptación mobile/responsive.** Auditoría completa de todas las páginas y componentes frente a los breakpoints del theme (`xs`/`sm`/`md`/`lg`/`xl`, D010) sin tocar la identidad visual ni la API de los componentes base de fase 04. La mayoría de las secciones de Home, la navegación (`Navbar`/`PageNavbar`/`MobileMenuOverlay`) y el Hero ya tenían un buen tratamiento responsive de fases anteriores (D032/D046/D050/D054, etc.) — el trabajo de esta fase se concentró en los puntos que sí se rompían en mobile: (1) **`DonationFormCard`** tenía un `width` fijo en píxeles sin variante responsive — en el modal de Diezmo (`TitheModal`, ancho 440) y en el de Aporte a proyecto (`ProjectContributionForm`, ancho 320 por defecto) esto desbordaba horizontalmente en cualquier viewport más angosto que el card, es decir, en la mayoría de los celulares — se corrige a `width: { xs: "100%", sm: width }`, sin cambiar nada desde `sm` en adelante; se agrega también `fullWidth` al `Dialog` de "Aportar" en `ProjectDetailClient.tsx`, que no lo tenía. (2) **`ProjectCard` layout `horizontal`** (usado en `/proyectos`, vista "lista", la vista por defecto) era una fila fija (foto 200px + texto + botón 120px) que no cabía en mobile — el cliente vio 3 opciones (apilar el layout / forzar grilla en mobile / scroll horizontal) y eligió **apilar**: por debajo de `sm` la foto pasa a ocupar el ancho completo arriba (180px de alto) y el contenido + CTA se apilan debajo a todo el ancho; desde `sm` en adelante el layout queda exactamente igual que antes. (3) **`DashboardTable`** (grid de 5 columnas, ~560px mínimo) no cabía en mobile — se armó un comparador visual con 3 opciones (tarjetas apiladas / scroll horizontal / columnas ocultas) y el cliente eligió **tarjetas apiladas**: por debajo de `sm` se oculta el grid de escritorio (sin cambios) y se muestra una lista de tarjetas verticales con los mismos datos y acciones (título+estado, barra de progreso, fecha de cierre, editar/eliminar). (4) **`DashboardProjectForm`**: los grids de 2/3 columnas fijas (monto meta/fecha, cada línea de presupuesto, nombre/rol y URL foto/Instagram de cada encargado) pasan a 1 columna por debajo de `sm` (con el campo principal de cada fila ocupando el ancho completo y el botón de eliminar en su propia fila cuando corresponde). (5) **`DashboardTopbar`**: se redujo el padding del contenedor y de las píldoras de navegación, el gap entre grupos, y se ocultó el divisor decorativo entre el toggle de tema y el avatar por debajo de `xs`/`sm` — sin esto el conjunto (logo + nav + acciones) quedaba al borde de desbordar en viewports de ~360px o menos; el topbar sigue siendo una sola fila en cualquier tamaño, no colapsa a menú hamburguesa (ya no aplica el sidebar vertical que sí lo necesitaba en D024/D025, reemplazado por el topbar en D059). El resto de componentes de fase 04 (Agenda bento, secciones de Home, Footer, formularios de contribución, tarjetas de líder/área/prédica/red social, etc.) ya cumplían el criterio "desktop-first, mobile es adaptación" (`DESIGN_RULES.md`) sin cambios adicionales | El cliente pidió explícitamente ver las opciones antes de decidir en los dos puntos con más impacto de UX (listado de Proyectos y tabla del Dashboard) — para el listado de Proyectos se le presentaron 3 opciones en preguntas de texto y eligió apilar el layout horizontal manteniendo ambos modos de vista disponibles; para la tabla del Dashboard pidió visualizarlas primero, así que se publicó un comparador HTML con mockups fieles a la paleta/tipografía del proyecto (mobile frame, toggle claro/oscuro) y eligió tarjetas apiladas. El resto de fixes (`DonationFormCard`, `DashboardTopbar`) son correcciones mecánicas de bugs de overflow ya presentes en el código, sin ambigüedad de diseño, así que se implementaron directo sin plantear opciones — siguiendo el mismo patrón `{ xs: ..., sm/md: ... }` ya usado en el resto del sistema (D032, D046, D050) en vez de introducir un criterio nuevo | Cerrado — implementado en `components/ui/DonationFormCard.tsx`, `app/proyectos/[slug]/ProjectDetailClient.tsx`, `components/ui/ProjectCard.tsx`, `components/ui/DashboardTable.tsx`, `components/ui/DashboardProjectForm.tsx`, `components/layout/DashboardTopbar.tsx`. Pendiente para una futura ronda: verificar en vivo en dispositivos reales (esta sesión solo pudo razonar sobre anchos de viewport, no tomar capturas del sitio corriendo) |

| D065 | Global/Componentes/Páginas | Ronda de feedback puntual post-fase 08, un lote de 11 items sueltos (mezcla de bugs mecánicos y decisiones de diseño). (1) **`Reveal.tsx`** gana un `amount` de disparo responsive (`viewport.amount`, `framer-motion`): 0.25 en desktop (sin cambios) pero 0.1 por debajo de `sm` — en mobile las secciones suelen ser más altas que el viewport y con el umbral de desktop la animación tardaba en dispararse o el usuario ya había scrolleado de largo. (2) **CTA "Ver canal de YouTube" (`Sermons.tsx`)**: el acento de hover pasa de navy (`primary.main`, D051) a naranja (`secondary.main`) — chocaba con el hover naranja de las `YoutubeEmbedCard` de la misma sección; el cliente pidió unificar en uno de los dos colores y se eligió naranja para que el botón "cierre" la sección con el mismo acento que ya usan las cards. (3) **`MobileMenuOverlay`** — dos fixes: (a) el fondo pasa de navy fijo permanente (D025/D026) a **theme-aware** (`background.default` en claro, `primary[900]` en oscuro) porque, al tocar el `ThemeToggle` DENTRO del menú fullscreen, el fondo no cambiaba y parecía que el botón no hacía nada (el resto del sitio sí cambiaba de modo, pero quedaba tapado detrás del propio menú) — esto reemplaza puntualmente el criterio de "navy fijo siempre" de D025/D026 solo para este overlay, no para el sidebar del dashboard (ya reemplazado por el topbar, D059) ni para `TitheModal`; (b) los links de tipo "página" (Inicio/Proyectos) ganan el mismo tratamiento de píldora con ícono y borde que ya tienen en escritorio (D054) en vez de verse idénticos a los links de ancla — antes ambos tipos se veían como texto plano subrayado, sin la distinción "esto te lleva a otro lugar" que sí existe en desktop. (4) **Modal de Diezmo (`TitheModal.tsx`)** — el `Paper` en `fullScreen` (D050) combinaba `overflow: "hidden"` con la altura fija `height: 100%` que MUI le da por defecto a todo Paper fullscreen: cuando el contenido (eyebrow+heading+descripción+contador+form) era más alto que la pantalla en mobile, quedaba recortado y sin forma de hacer scroll pese a `scroll="body"`. Se cambia a `height: "auto"` + `minHeight: "100%"` (el Paper crece con el contenido, `scroll="body"` scrollea la página del modal); además el párrafo descriptivo se oculta por debajo de `md` (`display: { xs: "none", md: "block" }`) para que el modal quede más compacto en mobile, quedando visible desde `md` donde sí hay espacio de sobra. (5) **`/proyectos` — se quita el toggle lista/cuadrícula** (agregado en D052): el cliente pidió una sola visualización; queda únicamente el layout `horizontal` (lista), que era la vista por defecto — se elimina el estado `viewMode`, los dos `IconButton` de alternar vista, y la rama de grid del contenedor. (6) **`DashboardProjectModal` pasa a `fullScreen`** (antes `95vh` centrado, D060): mismo criterio que `TitheModal` (D050) — ocupa toda la pantalla en cualquier tamaño en vez de un dialog centrado; el `DialogContent` conserva el ancho de lectura del formulario (`maxWidth: 640, mx: "auto"`) para que los campos no se estiren de borde a borde en desktop. (7) **`Hero.tsx`** gana `maxHeight: calc(1024px - var(--navbar-height, 72px))` junto al `minHeight` ya existente (D050) — en monitores muy altos/anchos el Hero a `100vh` se sentía excesivo; el cálculo resta la misma variable de altura del navbar que ya usa `minHeight`, así el total (navbar + Hero) no supera 1024px. (8) **`ServiceAreas.tsx`** — grid asimétrico quirúrgico quirúrgico para evitar huecos vacíos cuando 5 elementos no dividen exacto las columnas de `sm` (2 cols) o `md` (3 cols): en `sm`, fila 1 → items 1-2 normales, fila 2 → item 3 solo a ancho completo, fila 3 → items 4-5 normales; en `md`, fila 1 → item 1 (1 col) + item 2 (2 cols), fila 2 → item 3 a ancho completo (3 cols), fila 3 → item 4 (2 cols) + item 5 (1 col); desde `lg` (5 columnas, calzan exactas) cada card vuelve a 1 columna. El cliente especificó el patrón exacto tras una primera pregunta de clarificación (su descripción inicial tenía una contradicción sobre cuántos elementos cabían por fila). La misma lógica de "no dejar hueco" se aplicó a **Prédicas (`Sermons.tsx`)**, que con solo 3 items en el grid de 2 columnas (`sm`) dejaba el último huérfano — ahora ese último item ocupa el ancho completo en `sm`, volviendo a 1 columna en `md` (3 items, 3 columnas, calza exacto). (9) **`ConfirmDialog.tsx`** — más padding interno en título/contenido/acciones (de los defaults de MUI, 16-20px, a `px: 3.5`/`pt-pb` explícitos) — el cliente lo sentía apretado, en particular en el modal de eliminar proyecto que lo usa. (10) **Tipografía en pantallas ≥1920px** (`theme.ts`): se agrega un media query fijo `@media (min-width:1920px)` a los variants `h1`-`h4`/`body1`/`body2` de MUI, subiendo cada tamaño un peldaño (~15-20%, ej. h1 60px→72px, body1 18px→20px). **Limitación documentada en el propio código**: la mayoría de las secciones del sitio (Hero, ServiceAreas, Sermons, etc.) no usan estos variants de MUI — definen su tamaño de fuente en `sx` con valores fijos en px, que pisan cualquier tamaño de variant — así que este cambio sienta el criterio y cubre cualquier `Typography` sin `sx.fontSize` propio, pero una pasada para subir también los tamaños fijos de cada sección a partir de 1920px queda pendiente como una ronda aparte, si el cliente la pide | El cliente mandó una lista de 11 puntos sueltos en un solo mensaje, mezclando bugs claros (scroll roto del modal de Diezmo, hueco de columna en grids, tipografía chica en pantallas grandes) con decisiones de diseño explícitas (quitar el toggle de vista, elegir un color de acento entre dos opciones que él mismo propuso). Se implementaron directo los bugs mecánicos y las decisiones donde el cliente ya indicó qué elegir; para el único punto genuinamente ambiguo (el patrón exacto del grid asimétrico en 2 columnas, cuya descripción inicial se contradecía con tener 5 elementos) se preguntó con 3 opciones concretas antes de implementar, seguido de una segunda pregunta cuando el cliente aclaró que ninguna de las 3 capturaba lo que quería y describió el patrón exacto para ambos breakpoints (2 y 3 columnas) | Cerrado — implementado en `components/ui/Reveal.tsx`, `components/sections/Sermons.tsx`, `components/layout/MobileMenuOverlay.tsx`, `components/ui/TitheModal.tsx`, `app/proyectos/page.tsx`, `components/ui/DashboardProjectModal.tsx`, `components/sections/Hero.tsx`, `components/sections/ServiceAreas.tsx`, `components/ui/ConfirmDialog.tsx`, `theme/theme.ts`. Pendiente para una futura ronda: sweep de tamaños de fuente fijos (hardcoded en `sx`) de cada sección para pantallas ≥1920px, y verificación en vivo en dispositivos reales de todo lo implementado (esta sesión solo pudo razonar sobre anchos de viewport y lógica de código, sin capturas del sitio corriendo) |

| D066 | Componentes/Global | Ronda de feedback puntual sobre D065, 2 items. (1) **`TitheModal.tsx` — ancho incompleto en mobile**: el fix de D065 (`height: "auto"`, `minHeight: "100%"`, manteniendo `scroll="body"`) resolvió el scroll pero dejó un bug de ANCHO — el modal no ocupaba todo el ancho en mobile. Causa: `scroll="body"` le agrega al `Paper` la clase `paperScrollBody` de MUI (`display: inline-block`, pensada para centrar un dialog normal dentro del body), que pisaba el `width: 100%` que la clase `paperFullScreen` necesita — el Paper terminaba angosto, ajustado a su contenido, en vez de ocupar el viewport completo. Se quita `scroll="body"` del `<Dialog>` por completo; con el `scroll="paper"` default de MUI, el propio `Paper` `fullScreen` (ya 100% ancho/alto por su clase nativa) es el que scrollea internamente (`overflowY: "auto"`, agregado explícito en su `sx`) cuando el contenido no entra, sin ninguna clase adicional que le achique el ancho. (2) **Tipografía ≥1920px, sin efecto perceptible**: el bump de D065 solo tocaba los `typography` variants de `theme.ts` (h1-h4/body1/body2) — pero casi ninguna sección del sitio usa esos variants sin más: la inmensa mayoría de los textos fijan su tamaño con un `fontSize` literal dentro de `sx`, y un `sx.fontSize` siempre pisa el tamaño del variant, así que el cambio de tema no tenía ningún efecto visible en la práctica (limitación que D065 ya había documentado como pendiente). Se hace un sweep agregando la key `"@media (min-width:1920px)"` directamente a cada `sx.fontSize` literal relevante, sección por sección: `Hero.tsx` (headline, eyebrow, stats), `ServiceAreas.tsx`, `Sermons.tsx`, `Leaders.tsx`, `SocialLinks.tsx` (solo el bloque de heading eyebrow/h2/body, no las pestañas de cuenta ni los íconos del grid de posts), `Agenda.tsx` (solo el mismo bloque de heading, no los tamaños internos de `AgendaTile`, que dependen de si la celda es "feature"), `History.tsx`, `Footer.tsx` (título/links de cada columna, logo "Iglesia", tagline, "Conecta", email, barra de copyright), `Navbar.tsx` y `PageNavbar.tsx` (logo "Iglesia", links de ancla, píldoras de página vía `pillSx()`), y el heading de `app/proyectos/page.tsx` (eyebrow/h1/body). **Cobertura incompleta, documentada explícitamente**: el sweep cubrió Home completo + navegación global + Footer + el heading de `/proyectos`, pero deliberadamente NO tocó el Dashboard (`DashboardTable`, `DashboardProjectForm`, `DashboardStatBand`, `DashboardTopbar`, `app/dashboard/*`), el texto interno de `TitheModal`/`TitheForm`, `ConfirmDialog`, ni `ProjectCard`/`ProjectSidebar`/`ProjectDetailContent`/`MobileMenuOverlay`/`PhotoAnchorBand`/`LeaderCard` — queda como ronda aparte si el cliente la pide | El cliente reportó, sobre lo recién implementado en D065, que el modal de Diezmo seguía sin ocupar todo el ancho en mobile (el scroll sí funcionaba) y que no notaba ningún cambio de tamaño de tipografía en pantallas grandes — ambos apuntaban a que el fix de D065 había resuelto el síntoma equivocado o incompleto: el ancho fallaba por una combinación de props de MUI documentada como rareza conocida (`fullScreen` + `scroll="body"`), y la tipografía fallaba porque el cambio se aplicó a un mecanismo (variants de tema) que el resto del código no usa | Cerrado — implementado en `components/ui/TitheModal.tsx`, `components/sections/Hero.tsx`, `components/sections/ServiceAreas.tsx`, `components/sections/Sermons.tsx`, `components/sections/Leaders.tsx`, `components/sections/SocialLinks.tsx`, `components/sections/Agenda.tsx`, `components/sections/History.tsx`, `components/layout/Footer.tsx`, `components/layout/Navbar.tsx`, `components/layout/PageNavbar.tsx`, `app/proyectos/page.tsx`. Con esto el cliente da por culminada esta ronda de feedback post-fase 08. Pendiente para una futura ronda, si se pide: sweep de tipografía ≥1920px sobre Dashboard/modales/Project cards-sidebar-detail; verificación en vivo en dispositivos reales |